# Contract v246: mutation M6-C

状态：已实现。协议 `1.166`，save 容器 `v1`，State Hash Schema `v83`，内容包
`1.238.0`，内容 hash
`89c2898725b58fc6c56b7524f2c85e8465ef4e0096aa7de3ddef3fb5f225e1f8`。

## 运行时边界

- Flatulence 按原版固定命中点 `randint1(3000) == 13` 触发，以玩家等级为基础伤害，在半径 3 内造成毒素伤害。
- Attract Demon/Animal/Dragon 分别按原版 1/6666、1/7000、1/3000 触发；触发后才判定 1/6、1/3、1/5 友好，复用分类候选和角色自身的 `friends` 群体骰。
- Raw Chaos 以 1/8000 触发，以玩家等级为基础伤害，在半径 8 内造成混沌伤害。
- Eat Light 以 1/3000 触发：脚下永久光照治疗 10 点；非神器装备光源按当前燃料的 1/20 治疗并减半燃料；随后在半径 10 内造成 50 点黑暗伤害并熄灭永久光照。
- 区域熄灭是 `lighting.rs` 中来源无关的事务，可由后续物品和能力直接复用。

所有周期检查继续只在本地地图运行，并按 `sourceIndex` 排序。世界地图零触发、零额外 RNG。

## 兼容与验收边界

本批没有新增协议字段、存档字段或 State Hash 输入，故协议保持 1.166、State Hash
Schema 保持 v83、save 容器保持 v1。内容包升至 1.238.0，行为基线升至
contract-v246；现有 21 个 exact fixture 无需刷新。

聚焦测试覆盖原版触发点、毒素与混沌范围、三类召唤分类及友好控制、光照治疗、
燃料减半、50 点黑暗范围和区域熄灭。
