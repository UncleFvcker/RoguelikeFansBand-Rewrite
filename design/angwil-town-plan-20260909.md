# 下一座城镇：安格维尔实施计划

状态：A1、A2 已实施；A3 来源与交付缺口已核对，功能未开放；A4 未实施。A2 工作基线为 `codex/dungeons-towns@ffb2fd9a5`；规划时 main 为 `43f8afd1b`、物品分支为 `codex/realms-items@89286a6a3`；当前城镇包为 `1.384.17`。RFB 来源为 `D:/codex/Frogcomposband/master` 的 `master@a0d92b6378d148c5262cc236b8fa6ed2ca06a54c`，全部通过 Git 对象读取。每批实施前复核实际交付和来源，不预留版本号。

## 1. 推荐与范围

**下一座选安格维尔，先交付森林地表上的城镇与现有服务，再接庄园任务。** 规划前已有前哨站、莫里凡特、阿南巴、萨洛斯、泰尔莫拉，A1 已新增安格维尔；泰尔莫拉 T3/T4 的任务依赖仍独立跟进，不作为安格维尔基础补给与服务的前置。

`w_info.txt` 的索引 **4**、世界位置 **(74,23)**，中文城名逐字为“安格维尔”；`t_info.txt` 关联 `t_angwil.txt`。该格原版地表是 **TREES、等级 4、有道路**，不是普通城镇地板。剩余候选祖尔 (77,6) 的三塔混合任务、身份限制和领域服务，且商店组合不同；安格维尔可先复用更多现有能力。

本城有九类商店、Home、博物馆、旅店，以及 11 处非任务建筑服务；没有泰尔莫拉的任务条件折扣，也未声明重铸、赌博商店或猎人局。不自动复制其他城镇的按钮，不同时添加祖尔或邻近地牢，不扩大种族/职业入口，不要求先完成泰尔莫拉全部任务。

## 2. 已核对的地图和设施

原版模板为 **112×54**，共 6048 格，其中 **4194 格为空白**。`wild.c` 在荒野底图上以模板原点 **(0,0)** 叠加，`rooms.c` 跳过未定义的空格，因此空白保留底层森林；不能裁掉外侧任务区、居中平移为 (43,6)，或把空白统一改成地板/树墙。模板外继续使用现有 198×66 荒野视图，不扩大模板尺寸。

A1 已增加 `inlineMap.inheritWildernessTerrain`：模板未覆盖的格子由既有坐标与荒野种子生成，显式格子继续按模板覆盖；首次生成与可见城镇合成共用这一入口，地形和探索状态沿用同一城镇楼层保存。只临时复用本次生成的分块，不增加持久缓存、森林 RNG 或新地形类型。不透明城镇保持原有默认填充。内容校验仍检查楼层归属、模板边界及明确可站立的落点；开放森林模板不适用封闭城墙的门到道路静态校验。

以下均为模板内零起算坐标，符号含义来自 `t_pref.txt`：

| 符号 | 设施 | 坐标 |
| --- | --- | --- |
| `0/1/2` | 蘑菇店 / 杂货店 / 防具店 | (55,29) / (57,31) / (60,35) |
| `3/4/5` | 武器店 / 神殿 / 炼金店 | (65,32) / (10,21) / (18,19) |
| `6/7/8/9` | 魔法店 / 黑市 / Home / 书店 | (12,17) / (35,28) / (27,33) / (5,18) |
| `M` | 博物馆 | (12,35) |
| `a/b` | 图书馆 / 庄园 | (5,28) / (35,22) |
| `d/e` | 赌场 / 旅店 | (24,9) / (24,5) |
| `f/g/h` | 驯兽师 / 武器大师 / 战士公会 | (46,33) / (43,33) / (74,20) |
| `i/j/k` | 法师塔 / 内殿 / 盗贼巢穴 | (102,33) / (10,24) / (41,48) |
| `l/m/n` | 弓手酒馆 / 圣武士圣所 / 王牌之塔 | (56,17) / (78,23) / (88,39) |

任务入口未开放时按来源恢复：`z` (1,8)、`v` (67,49)、`x` (40,52) 为树；`w` (5,4) 为草；`u` (53,2) 为永久墙；`s` (28,44) 为泥土。来源虽声明任务 22 的默认字母 `y`，地图没有 `y`，庄园链也不提供 22，不凭注释补入口。

下表为来源基础价，双值为 Owner / 其他身份，最终扣费由既有 Rust 报价规则计算。中文建筑名、店主名逐字来自 `t_angwil.txt`。

| 建筑、店主 | 服务与基础价 | 身份与开放范围 |
| --- | --- | --- |
| 图书馆、瓦内里安 | 研究物品 1500、城镇纵览 0、单件鉴定 50 | 无任务折扣；注释中的种族传说不开放 |
| 庄园、科尔曼温 | 请求任务 0 | 随 A3 开放，不能称城堡或提前列出未交付后继 |
| 赌场、亨托 | 五种普通游戏和规则 | 复用 S6a；怪物竞技场尚未实现，继续隐藏 |
| 旅店、多切尔 | 住宿 25、餐饮 3、城镇传送 500、声望 1 | 餐饮要求 Member，原版全部种族为 Member；传闻 10 仍按既有文本边界隐藏 |
| 驯兽师、阿拉德雷斯 | 研究怪物 1500/1500 | 无额外条件 |
| 武器大师、罗东温 | 评估护甲 400/400 | 复用实际评估与扣费 |
| 战士公会、迦尔兹 | 武器/护甲强化，声明价 0 | 来源 Owner：Warrior、Samurai、Cavalry、Berserker、Blood-Knight、Mauler；0 仍走现有强化估价，不表示免费 |
| 法师塔、瓦尔塞隆威 | 全部鉴定 200/1000 | Owner 按职业：Mage、High-Mage、Blood-Mage、Sorcerer、Yellow-Mage、Gray-Mage；Ranger、Warrior-Mage 为 Member；不能复制莫里凡特的 Sorcery 领域条件 |
| 内殿、卡比尔 | 治疗 0/150、属性恢复 300/1000 | Priest 为 Owner、Paladin 为 Member；不能复制泰尔莫拉的 Life 领域 Owner |
| 盗贼巢穴、摩尔弗雷 | 住宿 0/50、全部鉴定 175/800 | Rogue 为 Owner |
| 弓手酒馆、凯勒盖尔 | 弹药强化 22/44、弓强化声明价 0 | Ranger、Archer、Sniper 为 Owner |
| 圣武士圣所、卡尔皮罗斯 | 护甲强化 240/440、治疗 0/100 | Paladin 为 Owner |
| 王牌之塔、科鲁斯 | 召回 0/150 | Trump 领域为 Owner、Amberite 种族为 Member；指定地牢层传送的来源动作已注释，不能复制该按钮 |

当前城镇分支正式职业为 Warrior、Cavalry、High-Mage、Paladin、Archer、Sniper。先绑定已存在的准确身份：例如 High-Mage 在法师塔为 Owner，Paladin 在内殿只是 Member；其余来源身份记录在导入声明中，待实际交付后绑定，不用相近职业替代。Amberite 的正式身份与 Trump 条件也按实施时内容复核。

## 3. 分批交付

### A1：完整模板、森林底图与基础补给（已实施）

注册 `demo.town.angwil`、`demo.floor.angwil` 和 (74,23) 世界地点，`mapOrigin` 为 (0,0)，保留 112×54 原图及其透明区域。拟用商店/设施前缀 `demo.shop.angwil-*`、`demo.town-facility.angwil-*`；实施前检查活动分支的 ID 占用。

接入九类商店和独立库存，复用现有店主/库存生成方式；Home 指向已有公共仓库，博物馆指向已有公共展品存储并沿用当前神器寄存限制。旅店开放上表四项已实现服务，只有实际到访后才进入城镇传送列表。沿用公共抵达位置与可站立校验，选择有依据的模板内落点，不修改新游戏出生城镇或为本城另写抵达规则。

尚未交付的建筑门保持关闭，任务标记保留上表默认地形。模板覆盖和存取须一起完成：只改初次渲染不能交付 A1。地表底图消费既有种子与生成结果，不把随机森林烘焙成一张永久固定替代图。

验收：完整模板尺寸、显式地形和透明区域；生成、滚动、再进入与保存恢复一致；普通商店实际买卖、跨城库存独立、仓库/博物馆共享、旅店收费与访问资格。复用已有城镇测试 helper；地图合成改变时覆盖一个已有不透明城镇，测试真实状态行为，不增加专项可达性或完整步行 E2E。

A1 交付：保留 112×54 完整模板与 1854 个显式格子；首次落点沿用 (99,33)，对应原文泥土。旅店基价为住宿 25、餐饮 3、声望 1、已访问城镇传送 500，传闻仍隐藏。多切尔及原文描述“精灵”记录在来源声明；普通旅店库存交易继续使用现有旅店商店模型的人类店主计价，不把描述字符串擅自映射成木精灵等另一正式种族。Home 共用前哨站仓库，博物馆共用萨洛斯馆藏并拒收神器。

A1 自动验证：城镇模块 64 项、荒野模块 16 项、世界模块 66 项通过；内容模板/边界检查及既有城镇内容检查通过，来源导入审计通过，town 类 6 个契约通过且未刷新。补正旧地图枚举中漏列的泰尔莫拉及本城位置，未改变其他城镇内容。生成 world Schema 并更新包与 lock；未改变协议、状态哈希 Schema 或新游戏出生设置。未构建桌面产物、未进行桌面或人工试玩验收。

### A2：11 处已有非任务建筑服务（已实施）

开放 B:0、3、5、6、7、8、9、10、11、12、13 的现有服务，逐项使用本城名称、店主、身份和基础价。图书馆纵览只描述已开放内容；法师塔按职业、内殿按职业、王牌之塔按种族/领域声明，不能整份照抄其他城镇的会员列表。B:7 不增加重铸，B:13 不增加注释掉的指定层传送。

验收重点：High-Mage 的法师塔 Owner 价与访客价；Paladin 在内殿按 Member 收费且 Life 领域不冒充 Priest；Trump Owner 免费召回和其他身份 150 基础价的实际召回事务；其他研究、评估、强化、鉴定与住宿复用已有覆盖。检查钱不足原子拒绝、报价等于扣款、恢复后设施身份和赌场会话归属正确。

A2 交付：开放上述 11 处建筑，全部中文名称、店主和基础费逐项按同一来源提交核对。只替换 11 个门格，A1 的商店、旅店、储物与 4194 格森林底图不变。图书馆纵览描述当前已开放设施；庄园门继续关闭。赌场沿用五种普通游戏及规则，没有怪物竞技场；战士公会没有重铸，王牌之塔仅提供召回。

身份范围：法师塔目前绑定 High-Mage Owner；内殿只绑定 Paladin Member，Life 领域不获得 Priest 身份。战士、弓手和圣武士建筑按现有准确职业绑定。未正式交付的 Mage、Priest、Rogue、Ranger 等来源身份保留在导入声明，不用相近身份替代。王牌之塔绑定 Amberite Member、Trump Owner；当前正式包没有 Trump 构筑，免费召回条件用有效的最小测试构筑验证，不代表 Trump 法术、构筑或新游戏入口已交付。

A2 验证：城镇模块 67 项通过，覆盖 High-Mage 非 Sorcery 领域仍为法师塔 Owner、访客收费、内殿治疗/属性恢复与钱不足原子拒绝、强化目标及扣款、研究/评估、盗贼巢穴住宿与鉴定。王牌之塔 Owner 免费、Visitor/Amberite Member 付费均验证了召回倒计时保存、实际入地牢和原位返回；赌场牌局验证本城会话归属、跨赌场操作拒绝和保存恢复。内容模板检查、完整来源审计、11 处费用/身份及地图差异核对通过；town 类 6 个契约通过且未刷新。正式运行时代码、协议和 Schema 未改动；本批未构建桌面产物或做桌面/人工试玩验收。

### A3：庄园首任务 19（已核对，待依赖交付）

来源为任务 **19、24 级、`q_doom1.txt`、清场**，权威中文名是 **“毁灭任务 I (安格威尔)”**；任务表使用“安格威尔”，城镇表使用“安格维尔”，分别保留，不擅自统一。地图 **47×11**，返回阶梯在 (11,9)，城镇入口 `z` 在 (1,8)。本首任务没有替代分支，不必先加入全部后继。

已确认 5 种怪物有正式定义：zombified-human、imp、black-knight、arch-vile、landmine。源图分别有 7、2、6、1、2 个标记；这只证明内容引用可复用，不代表群体、地雷及整图战斗已验收。另有 8 格 `PERMANENT_GLASS_WALL`、39 格 `DEEP_WASTE`，须核实通行、视线与地形伤害，不能全部换成墙/水。

地图物品含 elven cloak、blue dragon、治愈/重伤治疗药水、`BOLT, DEPTH+15`、`light crossbow, DEPTH+10`、强化护甲卷轴。默认结算为 `STAFF_TELEPORT`；Berserker 覆盖为 `OBJ(^ring$):EGO(elemental)`。当前计划未认定这些装备、装置及生成效果全部齐备；实施先按来源索引、中文表、真实生成等级、容量与效果逐项核对，缺少时列出具体交付，庄园继续关闭，不发替代奖励。

该任务奖励声明未要求固定神器或 `ART(*)`，不把泰尔莫拉随机神器灯当作当然前置；只复用首任务实际需要的共享物品生成和任务结算能力。接取、清场、提前离开失败、回庄园结算、重复领取拒绝和保存恢复均通过后开放；此时未交付的任务 36 仍不被选中。

A3 本次结论：**庄园继续关闭，不添加未能兑现的任务或替代奖励。** 核对基线为城镇分支 `a7af2aaf4`、main `df4e94476`、物品分支 `8af0fb8c9`；RFB 仍为前述 `a0d92b6378d148c5262cc236b8fa6ed2ca06a54c`。以下是实际代码与 Git 对象的差异，不把其他分支的阶段标题当作本任务依赖已交付。

| 交付项 | 来源与当前差异 | 接入前需要的具体结果 |
| --- | --- | --- |
| `STAFF_TELEPORT` 固定效果法杖 | `init1.c::_parse_effect` 选择法杖底材 k313、TV/SV 55/0 与 `EFFECT_TELEPORT`；三个核对分支均没有对应正式装置。当前已有普通长距离传送效果，不能直接把卷轴当任务奖励 | 正式装置、同源显示名、固定效果初始化、充能/使用/保存；效果与使用耗时走共享装置执行路径 |
| 蓝龙鳞甲 | k293、TV/SV 38/2，源中文表为“蓝龙鳞甲”；三个分支都缺该底材。原始 AC 40、to_a +10、命中 -2、电抗、四元素破坏免疫，激活 `BREATHE_ELEC:30:30:150` | 导入真实底材、属性与闪电吐息；接入地图固定基础物品的实际生成。E8.3 的龙牙/龙翼/龙靴/龙盔/龙皮手套/龙皮盾生成明确排除龙鳞甲，不能冒充此项 |
| 地图物品生成 | `inline_item_instance` 使用通用 Ego materializer，未表达本图逐格生成深度；inline loot 仅有 table ID 且使用整层深度。固定 `elven cloak`、`blue dragon` 也须执行对应基础生成，而不是仅摆放静态实例 | 复用共享物品生成上下文，支持本图 k196 精灵披风、k293 蓝龙鳞甲，以及 39 级 `BOLT` 类别抽样和 34 级 k163 轻弩的 `AM_GOOD` 生成；不重复写装备公式或固定成一支普通弩矢 |
| 永久玻璃墙 | f219 中文“永久玻璃墙”；阻挡移动和通常投射、透视、永久，不能穿墙。现有 `glass-wall` 允许穿墙；现有永久墙又遮挡视线 | 用现有地形表达组合定义准确的永久透明墙，覆盖移动、视线、投射和破坏边界，不替换成普通墙或普通玻璃 |
| 深层毒废料 | f229 中文“有毒废料”，`ACID | DEEP`，可通行、可视、可投射且无 `DROP`。`surface-waste` 是无伤害荒野地表；当前 hazard 入口仅处理世界图/荒野层，不会执行任务层毒废料规则 | 将实际规则放进现有地形/回合职责，覆盖酸伤、中毒、抗性、漂浮、无敌、再生抑制及掉落限制；不能仅复制一个绿色可走格 |
| 怪物与清场 | 五种正式 actor ID 可复用；此图没有 FRIENDS/ESCORT 来源声明，不以群体生成底座作为当然前置。地雷已有不可移动、隐形、25d2 自毁近战定义和自毁死亡路径；黑骑士来源 `HEROISM` 未出现在当前正式施法列表 | 补齐已观察到的黑骑士能力差异，核对 Imp/黑骑士初始睡眠与 `FORCE_SLEEP` 的区别，并在本图验证地雷自毁、arch-vile 复活产生的怪物与清场结算，不能只按 18 个初始标记判定胜利 |
| 庄园与任务结算 | 当前还没有任务 19 定义或庄园任务服务。通用 task reward 使用固定条目/通用 Ego materializer，需明确衔接固定装置、来源和完整鉴定 | 复用既有接取、clear-floor、提前退出失败、奖励空间检查/单次领取和保存状态；接入 B:1 科尔曼温的庄园及 z 门，仅交付任务 19，后继 36/91 不提前暴露 |

奖励细节：`quest.c::quest_get_reward` 把未声明深度的奖励 offset 设为 1，因此传入房间物品构造器的等级为 **25**。但 `STAFF_TELEPORT` 走 `device_init_fixed`，本例 `forge.level = 1 - 1 = 0`；默认效果 power/difficulty 来自效果表的 **20**，基础消耗 **10**、满能量 **60**，不能把物品构造等级 25 当作装置 power。使用执行 `teleport_player(100, 0)` 并把使用能量乘 3/2，Astral Guide 等现有身份修正须按实际能力处理。结算要完整鉴定并保留 `ORIGIN_QUEST_REWARD` 与 q19 来源，不能只返回一个未知的通用物品。

地图物品明细：精灵披风 (2,4)、蓝龙鳞甲 (21,8)、治愈药水 (32,9)、两个 `BOLT, DEPTH+15` 点 (43,7)/(43,8)、轻弩 `DEPTH+10` (43,9)、两瓶治疗重伤药水 (8,8)/(14,8)、两张附魔防具卷轴 (8,2)/(14,2)。两个弩矢点代表两次物品生成，数量服从来源，不等于两支弩矢；已有药水及 `demo.item.armor-tempering-scroll` 的真实效果可以复用。无 DEPTH 的地图固定物品按层级 24 生成，不套用任务奖励的 +1。

源图另有 **3 格 `%`**：(33,5)、(44,6)、(27,9)。它们不是空白地板；`rooms.c` 的历史默认分支调用 `place_outer_noperm_grid` 放置非永久外墙。全图仍为 47×11，215 个 `#`、8 个 `W`、39 个 `~`、4 扇门、18 个怪物标记及 (11,9) 返回阶梯；接入时保留这些区别。

毒废料规则核对到 `dungeon.c:1403–1451`：非无敌时以 2/3 概率处理；深层原始酸伤为 `1400 + randint0(800)`，漂浮先除 15，中毒量取酸伤原值的 6/5，再分别计算酸/毒抗性、除 100 并随机舍入。还涉及护甲受损减半、添加中毒、禁止自然恢复及 1/32 的体质损失抗性检查；漂浮并非完全免疫。实施时复用现有伤害、抗性、状态和属性扣减路径，不能简化成固定酸伤。

Berserker 覆盖为 25 级戒指、强制 Ego 201 `(Elemental)`，使用 `GOOD | GREAT | FORCE_EGO`；当前没有正式 Berserker 身份，记录此覆盖但不把 Warrior 视为 Berserker。任务没有显式固定神器或 `ART(*)` 要求，不额外强制等待泰尔莫拉随机神器灯。普通装备生成仍须按已交付的共享机制说明实际支持范围。

本次仅完成来源/依赖审计和清单，**未进行任务接取、战斗、结算或保存的游戏验收**。未修改正式内容、包版本、Schema 或运行时代码；只校验文档与源数据。`q_doom1.txt` 保留 Jeff Coleburn 原作、Gwarl 为 ComPosband 7.0.4 重制及 FrogComposband 修改署名；将来导入地图/叙事前仍须按该材料的适用声明核对，不能套用 `t_angwil.txt` 的地图修改说明。本次不复制完整地图或任务叙事。

### A4：庄园后续任务，保留三个替代组（已核对，待分组实施）

`t_angwil.txt` 正常顺序是 **19 → 36/91 → 35 → 24/86 → 21/85 → 23 → 0**，前项 Finished 或 FailedDone 均可继续；不能按编号排序，也不能默认只保留每组第一张地图。入口分别为 `s`、`v`、`u`、`w`、`x`，替代任务共用对应逻辑任务入口。

| 逻辑任务 | 已核对的来源与分支条件 | 后续核对重点 |
| --- | --- | --- |
| 36 沙坑 (安格威尔) | `q_worms.txt`；`quest_seed % 34 >= 17` 替换为 91 `q_pit2.txt`（绝望之坑） | 默认 `blade of chaos, DEPTH+12`；Pumpkin 覆盖为费诺提灯 |
| 35 飞龙任务 (安格威尔) | `q_dragons.txt`，42 级，清场 | 默认 dragon wings；Death-Sword、Filthy-Rag、Pumpkin 有不同装备/装置奖励，按准确身份与最后匹配规则核对 |
| 24 微软的兴衰 (安格威尔) | `q_microsoft.txt`，50 级，击杀 bull gates；`quest_seed % 66 >= 33` 替换为 86 `q_mug.txt`，45 级、危险 55，击杀 gospel of mug | defender 戒指/护符及 Pumpkin 药水覆盖；两张任务文件覆盖条件顺序不同，不能共用未经核对的奖励表 |
| 21 毁灭任务 II (安格威尔) | `q_doom2.txt`，55 级；正常荒野且 `quest_seed % 52 >= 26` 替换为 85 `q_doom2c.txt` | 默认 `STAFF_HEAL_CURING`；本体有 Berserker 药水覆盖，替代图未声明该覆盖，不能自动复制 |
| 23 孵化深坑 (安格威尔) | `q_spawning_pits.txt`，65 级、危险 80，清场 | 深度偏移随机怪物/物品；默认 balance dragon scale mail，`RANDOM23 % 77 == 7` 为 power dragon scale mail，Politician 最后覆盖为 sexy swimsuit；不得把 RANDOM23 错当 RANDOM0 |

三个替代组复用现有任务选择与存档权威状态，并记录与原版 `quest_seed` 取模的适配差异；进城、开面板和读档不重投。逐组交付真实地图、目标、物品效果和奖励，验收两分支及后继衔接后才开放下一组。此表是实施核对入口，不代表全部来源怪物能力、随机生成、奖励与叙事已经审计完成。

A4 本次结论：**完成八张地图及分组依赖核对，尚未开放后续任务。** 核对基线为城镇分支 `9b727b9b3`、main `df4e94476`、物品分支 `8af0fb8c9`；RFB 来源提交仍为 `a0d92b6378d148c5262cc236b8fa6ed2ca06a54c`。A3 首任务仍待交付，不能绕过前继直接开放任务 36。以下区分当前可复用内容、其他分支已交付内容和仍缺少的机制，不把八张图一并声明为可玩。

| 来源任务/地图 | 尺寸、返回阶梯（图内坐标） | 实际地图要求 |
| --- | --- | --- |
| 36 / `q_worms.txt` | 35×24，(17,11) | 560 格碎石、45 格花岗岩；4 种怪物共 10 个标记，清场。不能把碎石铺成普通地板 |
| 91 / `q_pit2.txt` | 35×23，(33,11) | 532 格碎石；5 种怪物共 6 个标记，清场；Deep one 声明 `FRIENDS(1d5)`，实际生成数不等于标记数 |
| 35 / `q_dragons.txt` | 52×32，(25,18) | 21 种龙共 82 个标记、20 扇关门，清场；对应正式 actor ID 均存在，仍须验证各类吐息与整图战斗 |
| 24 / `q_microsoft.txt` | 79×55，(1,53) | 击杀 Bull Gates；10 扇 `LOCKED_DOOR_7`、308 个随机陷阱、860 格浅岩浆、226 格深岩浆；另有 `ICKY`、金币、JUNK 和 SKELETON 类别生成。Software bug 有繁殖规则，目标不是全图清场 |
| 86 / `q_mug.txt` | 49×24，(4,22) | 击杀 The Gospel of Mug；3 扇 `LOCKED_DOOR_4`、1 个警报陷阱、81 格深岩浆及 `ICKY`；15 次 BOOK、1 次 HI_BOOK、1 次镰刀 `DEPTH+10` 生成。目标与 Tracking pixel 明确 `NO_SLEEP`；Raal's Tome 正式 ID 为 `demo.actor.raal-s-tome-of-destruction` |
| 21 / `q_doom2.txt` | 69×29，(1,8) | 9 种怪物共 112 个标记、21 扇关门、35 次 `OBJ(*, DEPTH+1)`；Manes、Flying skull 明确 `NO_GROUP`；清场要处理 Arch-vile 复活后的状态 |
| 85 / `q_doom2c.txt` | 69×53，(34,26) | 7 种怪物共 36 个标记、998 格深岩浆、32 次 `OBJ(*, DEPTH+1)`；8 个 Arachnotron 标记明确 `NO_GROUP`，不能因其种族有 FRIENDS 而再扩群 |
| 23 / `q_spawning_pits.txt` | 69×31，(29,1) | 5 种 Quylthulg 共 30 个固定标记；17 个 `9` 各生成物品与随机怪物，3 个 `8` 各生成随机怪物，另有 1 个固定效果 `ROD_LITE_AREA`。清场须覆盖召唤产生的实际存活怪物 |

地图等级使用 `quest.c::quest_generate` 的 `q->level`，不使用 `danger_level`：91 的生成基准是 38，86 是 45，23 是 65。任务 23 的 17 次随机物品按 **72** 级、17 次随机怪物按 **74** 级、另 3 次随机怪物按 **76** 级生成；危险 80 不能加进这些偏移。两张 Doom II 的随机物品按 **56** 级生成，q_mug 的固定镰刀按 **55** 级生成。`OBJ(*)`、BOOK/HI_BOOK、固定底材是不同入口，不用一个静态物品列表替代全部来源抽样。

| 交付组 | 奖励及现有内容 | 接入前的具体缺口 |
| --- | --- | --- |
| 36/91 | 两图默认 k73 混沌之刃，构造等级 **50**；Pumpkin 为普通费诺提灯，等级 **39**。两种底材当前已存在，本奖励没有显式 `ART(*)` | 接入 A3 同一真实装备生成上下文和奖励鉴定/来源；固定混沌之刃不能被其普通分配等级 70 过滤掉。inline actor 当前逐个生成，须让替代图 Deep one 复用群体生成规则，再验证两图清场与后继 35 |
| 35 | 默认 k200 龙翼、Death-Sword 为 k78 龙牙、Filthy-Rag 为 k302 Gold Dragon Scale Mail + 强制 Ego 87 `(Attack)`，构造等级均 **43**。物品分支已提供上述底材、Ego 87 和龙系生成；城镇分支尚未集成 | Pumpkin 的 `WAND_BREATHE_COLD, DEPTH+5 | TYPE=capacity` 仍缺正式固定效果魔杖；Capacity Ego 251 已在物品分支，不能用普通冰球魔杖顶替。结合准确种族身份和最后匹配顺序接入奖励；验证龙翼等实际生成属性，不能仅复制静态 JSON |
| 24/86 | 默认戒指、Hydra 护符均强制 **首饰 Ego 200 Defender**；当前同名 `rfb-legacy.affix.defender` 是武器 Ego 18，不能复用。物品分支已有 `defender-jewelry`；Pumpkin 的 `potion of *healing*` 已存在 | 两图分别使用构造等级 **60/55**。本体覆盖顺序为 Pumpkin → Hydra，替代图为 Hydra → Pumpkin，保留来源顺序。接入逐格锁强度、陷阱抽样/警报、金币和书籍类别生成、`ICKY` 与任务层岩浆，验证两个指定目标及后继 21/85 |
| 21/85 | 默认 `STAFF_HEAL_CURING`；本体 Berserker 为 **6–9 瓶**治疗药水（`5+1d4`），替代图没有该覆盖 | 三个核对分支均缺对应正式法杖。接入固定效果装置、随机奖励数量；保留逐标记 `NO_GROUP`，验证复活/清场和替代图岩浆。不能复制本体的 Berserker 覆盖到替代图 |
| 23 | 默认 k304 平衡龙鳞甲；独立种子条件覆盖为 k305 力量龙鳞甲；Politician 最后覆盖为 k260 性感泳装，中文按 `kind_name_zh.inc`，构造等级 **66** | 三个核对分支均缺这三种底材及对应实际能力，也缺地图 `ROD_LITE_AREA`。接入逐格随机怪物/物品等级、接取时保存的奖励分支条件、准确 Politician 身份和最后覆盖，验证召唤清场与链尾归零 |

固定效果装置不能使用构造等级冒充 power：飞龙任务的 `WAND_BREATHE_COLD` 构造等级是 47，但来源 `device_init_fixed` 得到 **power 54**，再施加 Capacity 并充满；任务 23 的 `ROD_LITE_AREA` 构造等级是 91，但固定效果 **power 35**。Doom II 的法杖构造等级为 56，固定效果 **power/difficulty 55**；无增幅时治疗 **250 HP**，清除失明、流血、混乱、眩晕并结束超级狂暴，来源此效果没有清除中毒，不能套用一个泛化的“全部治愈”。容量、失败率、恢复与使用效果须由共享装置机制表达，不能只写奖励名称。

随机状态分为两件事：`init1.c:5251–5252` 中 `$RANDOM0` 读取玩家 `quest_seed`，`$RANDOM23` 读取任务 23 自己的 `seed`；后者在 `quest.c::quest_take` 接取时生成。现有三个替代组机制在初始化时按 `(game_seed ^ hash(group_id)) & 1` 选定并保存在任务状态，保留该权威路径即可，但它与原版三个模数共享同一 `quest_seed` 的结果、组间相关性不逐种子等价。任务 23 的奖励条件不能拿组选择位代替，也不能到领取时重新按 1/77 抽奖；应在接取时保存对应条件，进城、报价、拒绝领取及读档均不重投。

当前 `TaskRewardDefinition` 只有固定数量、加权条目及职业覆盖，缺种族条件、有序跨身份覆盖、逐任务生成等级和上述接取种子条件；只应为本组实际行为扩展现有奖励/任务状态，不另建通用条件脚本。所有源图固定怪物都能找到正式 actor ID，但引用存在不等于能力已验收：群体、初始睡眠、繁殖、复活和召唤须按各图实际声明验证。现有遭遇生成已有群体路径；逐格任务地图尚未接入。inline floor 目前也把 `vault_cells` 初始化为全 false，不能声称已保存 `ICKY`；任务层不会进入当前荒野岩浆 hazard，不能只引用荒野地形 ID。

实施仍按 **36/91 → 35 → 24/86 → 21/85 → 23** 分组，每组在前继已可结算后接入。验证两分支的接取、提前离开失败、清场或指定击杀、回庄园结算、满包拒绝不改变奖励、重复领取拒绝、保存恢复及 Finished/FailedDone 后继选择；未交付组保持不可选。正式身份缺失时保留来源待办，不把相近职业或种族当作覆盖身份。

本批仅修改计划并核对 Git 源数据，没有添加任务、改包版本/lock、修改 Schema 或运行时代码；八图尺寸、阶梯、固定怪物计数、特殊格子、正式 actor 引用及三个分支的奖励缺口检查通过，**未进行八张地图的游戏验收**。来源署名按文件分别保留：q_worms/q_dragons 标记 clouded，q_pit2/q_mug 标记 FrogComposband 7.1.chocolate 新任务，q_doom2/q_spawning_pits 标记 Jeff Coleburn，q_doom2c 标记 clouded 版本及后续修改；q_microsoft 没有因此获得其他地图的授权。导入完整地图或叙事前逐材料核对适用声明，本批不复制完整地图或叙事。

## 4. 边界、来源与验证

- 城镇方向负责模板叠加、商店/设施、任务调用与状态；物品/职业方向只提供实际缺少的装备、装置和身份能力。发现真实并发重叠再交接，不因共享文件预先加审批或另建公共底座。
- 主要改动落点为正式包的 towns、shops、townFacilities、worlds、locale、来源 selection，以及实际受影响的荒野合成/存取和城镇/任务模块。类型变更生成相应 Schema；不手改生成物。内容变化更新正式包与 lock。
- `t_angwil.txt` 带 Brett Flannigan 署名及后续修改说明；导入保留来源归属。地图与任务叙事按各自适用声明核对，不把代码文件头或历史批次判断自动套用到新材料；中文名称按权威表逐字使用。
- 邻近已有地牢包括 `demo.dungeon.dragon-lair` (74,28)、`demo.dungeon.graveyard` (85,19)、`demo.dungeon.castle` (88,34)。本计划不新增地牢、不保证几何近邻之间存在安全路线。
- 常规检查按实际改动选择内容/来源验证、城镇/任务/保存测试和相关契约。A1 触及共享合成/存取时检查实际受影响的现有调用者；公共初始化、状态哈希输入或 RNG 改变才扩大对应契约范围，不因新城镇一律全量刷新。
- 不安排专项路径可达性或完整步行—交互—存档—回城 E2E；布局由用户试玩反馈后调整。桌面、Android、全量前端不作为每批默认检查；需要可玩桌面产物时使用 Tauri standalone 构建。

推荐交付顺序：**A1 森林模板与补给 → A2 已有服务 → A3 首任务 → A4 后续任务分组**。前两批完成可称“安格维尔补给与非任务服务已开放”；庄园任务另报实际开放范围。A1、A2 已交付，下一步先完成 A3 上述具体依赖，再接入首任务；A4 已列出逐组缺口，随后按组实施，不将来源审计写成游戏验收。
