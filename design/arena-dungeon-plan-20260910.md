# 竞技场地牢生成与接入计划

状态：**AR1–AR5 已完成**。正式开放 (67,7) 入口、50–80 层、两名守卫与创造神器卷轴奖励；真实入口、全层往返、战斗、奖励使用、保存恢复及聚焦自动验收已通过。

## 1. 目标与选择

下一座接入原版 **竞技场（Arena，25）**：从地表入口进入 50–80 层，探索圆形房间与通道，击败最终守卫，获得并实际使用「创造神器」卷轴，再返回地表。保存恢复不得改变楼层、装备实例或重复发奖。

- 工作树：`D:/codex/RoguelikeFansBand-Rewrite-dungeons-towns`；分支 `codex/dungeons-towns`；规划基线 `b66a317a679f717b357b7704cafd5f21202c5968`，已同步三方向合入后的 main。
- 当前正式包 `1.410.0`、协议 `1.247`、State Hash Schema `120`、active 契约 `contract-v322`。黑暗洞穴／灾难区 D1–D5 已完成；世界定义现有 31 个地牢条目。
- 来源：`D:/codex/Frogcomposband/master` 的 `master` Git ref，本次实际提交 `a0d92b6378d148c5262cc236b8fa6ed2ca06a54c`。实施时再次记录实际来源提交；使用 `git show` / `git grep` 读取 Git 对象。
- 选择理由：竞技场已有来源登记和两名正式守卫，最新合入的随机神器工厂已解决主要奖励底座依赖。新增工作集中于圆形房间、按房间分配怪物及卷轴使用闭环。奥林匹斯山仍需独立处理神系与特殊生态，安格班涉及更长的主线与终局流程，均不并入本批。
- Rust `Game` 继续负责规则、状态、RNG 和 ID。复用现有生成、怪物分配、物品使用、奖励和楼层保存模块，不另建竞技场规则系统。

## 2. 已核实的来源

| 项目 | 原版定义及接入目标 |
| --- | --- |
| 身份与名称 | 25 / `Arena`；中文名「竞技场」，沿用 `demo.dungeon.arena` |
| 坐标 | `P:7:67` → **(67,7)** |
| 深度 | 50–80，**31 层**；80 层无向下通路 |
| 地板 L 行 | FLOOR 100；其余权重 0；通道参数 8 |
| 墙 A 行 | PERMANENT 100，MOUNTAIN_WALL 0，GRANITE 0；外墙 PERMANENT、内墙 GRANITE；两种 streamer 均 NONE |
| 标记与偏好 | `NO_VAULT`、`BIG`；monster divisor 0；无 M 行偏好 |
| 入口守卫 | 691，龙魔像，`demo.actor.drolem` |
| 最终守卫 | 1110，散失金属史莱姆，`demo.actor.metal-babble-unique`；不能误用非唯一种 871 |
| 最终奖励 | `FINAL_OBJECT_70_52`；kind 364，`Artifact Creation:engsno argsan`，中文名「创造神器」 |

来源入口为 `lib/edit/d_info.txt`、`src/dungeon_name_zh.inc`、`src/monster_name_zh.inc`、`lib/edit/k_info.txt` 与 `src/kind_name_zh.inc`。物品沿用已有来源身份 `artifact-creation-engsno-argsan`，不自行另译名称或重建身份。

### 生成与规则的实际边界

- **专用圆形房间：**`src/rooms.c::room_build` 对 `DUNGEON_ARENA` 调用 `build_type16(TRUE)`。半径 `3 + randint0(5)`，取 3–7；用原版 `distance` 判断圆形内部，地板范围为距离 ≤ 半径−1，外围经 `place_extra_bold` 与 `add_outer_wall` 处理。不是普通 `DF1_ARENA` 开阔布局，也不是城镇竞技场设施。
- **通道仍存在：**`src/generate.c` 仍打乱房间顺序并连接房间；按 `randint1(depth) > tunnel_percent` 选择洞穴式或普通通道。`src/grid.c` 的生成通道依赖生成网格类别，不能直接套用游戏内“永久墙不可挖掘”来推断房间断连。旧计划中的“隔离房间”不作为本批实现依据。
- **房间照明：**未受 DARKNESS 限制时，`randint1(depth) <= 15` 决定房间亮起；本地牢未设 DARKNESS。不能把全部房间固定为亮或暗。
- **中心单怪：**每个成功建成的房间在中心调用 `get_mon_num(depth)`，并以 `PM_ALLOW_SLEEP` 放置一个候选。不附带普通房间群体；候选为空或放置失败的源行为需保留明确处理。
- **分配下限：**`src/monster2.c::get_mon_num` 在普通、非召唤分配上下文要求怪物等级 ≥ `MIN(50, depth-5)`，故 50 层为 45，55 层起为 50。沿用既有稀有度、唯一种和分配资格，不能误写为 `MAX`，也不能把下限施加到全部召唤。
- **普通生成预算：**`src/generate.c::_cave_gen_monsters` 跳过额外普通怪物生成；`_cave_gen_objects` 跳过普通地面物品、金币及补给等流程。房间中心怪物、最终守卫、怪物死亡掉落与最终奖励仍分别处理。
- **不能顺带禁用：**源码未在 `_cave_gen_traps` 整体排除竞技场，也未给本地牢 `NO_DOORS`。`alloc_monster` 仍存在运行期分配路径，但竞技场排除群体／horde 分支；“不额外生成初始怪物”不等于“关闭环境刷怪”。
- **移动规则：**`src/melee2.c` 的竞技场特判涉及穿墙／毁墙怪物的寻路流，不能据此全局删除其移动能力。永久墙行为及怪物寻路按实际消费者核对。

## 3. 当前边界

`legacy-wilderness-selection.json` 与正式世界内容已同时开放竞技场。AR1–AR2 的圆形房间、中心单怪、零普通地面战利品预算及运行期生态已绑定到 31 层；地图采用 96×33、六房间预算与既有通道／门适配，不宣称复刻原版地图尺寸或 RNG 序列。

AR3 已完成正式卷轴、合法目标投影、已有物品原位造物、命名、消耗与持久化；AR4 已验证守卫奖励取得后使用同一入口。Android 装备经验尚无消费者。AR4 的战斗测试明确授予等级、强化装备和保护状态，每轮补满生命并移至守卫邻格；保留守卫原始生命、防御、抗性、AI 和真实攻击指令，不表示自然练级或人工通关。AR5 已完成聚焦自动验收，本批无可玩 EXE 构建。

## 4. 实施顺序

### AR1：圆形房间、墙与通道

**已完成。**新增内容枚举 `arena-rooms` / `circle`，沿用既有自由放置、预算和连通算法。半径 3–7、RFB 距离地板、永久填充／外墙、打乱房间顺序连接及通道选择 `randint1(depth)>8` 已落实；房间及其相邻外墙按 `15/depth` 亮起，DARKNESS 仍抑制照明。普通地牢不新增 RNG 抽取。

本阶段在 **96×33、6 房间、地板面积预算 800** 的大地图配置上验收；自由放置沿既有预算与有限尝试，必要时沿用原有分区放置。可放尺寸中先等权选半径，再选位置。洞穴式通道采用本项目现有逐格转向路径，普通通道采用 L 形路径；普通门沿现有单门适配，从真实直通道选合法位置，不复刻原版通道宽度、门数量或随机数顺序。圆形房间没有内部隔墙，A 行的内墙 GRANITE 不另造无消费者字段。NO_VAULT、无 streamer 和无其他地形布局由内容边界校验保证，保留普通门和现有陷阱流程。

来源仍为上列 `a0d92b6378d148c5262cc236b8fa6ed2ca06a54c`，本批补查 `src/grid.h` 的 CAVE_EXTRA／CAVE_OUTER 标记及 `src/rooms.c::add_outer_wall`，确认生成阶段可开通永久材质墙。验证使用现有楼层的最小测试配置，正式内容与入口留给 AR4；本阶段无包内容、协议或保存格式变化。

- 在现有 procedural 内容类型和生成模块中加入表达上述几何所需的最少能力，复用房间中心、占用格与连接流程。只为实际差异增加字段或枚举，不建立通用可编程生成管线。
- 对照 `place_extra_bold`、`add_outer_wall`、`build_tunnel`／`build_tunnel2`，落实地牢填充墙、房间边界和通道开口。生成阶段的开路与游戏内挖掘权限分别遵守各自约束。
- 落实 BIG、NO_VAULT、无 streamer、普通门与房间照明；不混入 CAVERN、河湖、废墟、普通 ARENA 开阔布局。地图尺寸沿本项目既有大地图适配，记录实际尺寸，不宣称像素或 RNG 序列逐格复刻原版。
- **完成证据：**固定种子复现圆形地板、半径边界、永久墙与通道；不同种子有有效变化；楼梯、玩家和守卫预留位置可达且无重叠。生成结束后的永久墙仍不可被普通挖掘破坏，保留已有普通地牢生成覆盖。

### AR2：房间中心分配与运行期生态

**已完成。**每个成功房间中心执行一次全局分配，候选为空时按源跳过，不再按 encounter rolls 添加第二批初始怪物。入口偏移至首房间中心左侧，楼梯避开所有中心，额外最终守卫预留在远端中心右侧；占位冲突显式报错。内容边界要求 actor slots 等于房间数加守卫等预留，落地战利品预算为零，并保留全局 encounter table；不接受巢穴和额外固定补给。陷阱、怪物携带物与死亡掉落继续走原消费者。

非召唤分配在原有增深之前的层级上应用 `MIN(50, depth-5)`；环境刷怪保留此限制，召唤保留独立资格与独立召唤次数。初始、环境与召唤的伴随／护卫均禁用，既有核心没有独立 horde 生成入口，本批不新增该机制。永久墙通行、毁墙与逐步寻路沿用现有地形判断；最终守卫能在普通地板和浅水移动，不能穿永久墙。

首领核验补齐了真实战斗缺口：`resist-all` 进入共享法术伤害结算，包含精神之矛；金属泡泡怪两种身份的百分之一减伤由近战扩展至箭矢和怪物近战，保留正伤害归零后的三分之一概率 1 点伤害。高防御与 3–6 HP 已有真实属性消费者，击杀后仍登记唯一种并掉落携带战利品。本阶段不将此掉落称为 AR3／AR4 的创造神器卷轴奖励。

来源仍为 `a0d92b6378d148c5262cc236b8fa6ed2ca06a54c`，补查 `monster2.c::get_mon_num/place_monster_aux/alloc_monster`、`xtra2.c::mon_damage_mod/mon_damage_mod_mon`、`gf.c::_BABBLE_HACK` 与既有移动消费者。自动验证覆盖 50／55／80 层分配边界、每房间单怪和守卫共存、零普通地面物品／金币、真实环境分配、低等级召唤与击杀掉落。核心 1301 项通过、3 项既有忽略；强化后的生成／生态与守卫移动断言另行定向通过。内容 world 61 项、26 条 active 契约、相关 Clippy、格式和 source/lock 验证通过，无 fixture 刷新。正式包、Schema、协议与存档格式不变，无本批桌面构建或人工试玩。

- 按成功生成房间的中心分配单怪，复用当前全局候选与唯一种登记；处理占位冲突和最终守卫预留，不能先放中心怪再无声覆盖。房间大小、数量与布局成为初始普通怪物数量的来源。
- 实现 `MIN(50, depth-5)` 的非召唤分配下限，以及本地牢禁用队伍伴随／horde 的源差异。保留运行期分配，避免为了取消额外初始怪物而移除整张 encounter table。
- 取消额外初始普通怪物及普通落地物品／金币预算，保留陷阱、怪物掉落和奖励消费者。复用既有预算校验；若零预算不合法，只在对应内容边界补足真实语义。
- 核实并复用穿墙／毁墙与寻路约束；最终守卫已有 actor 定义，但须检查其高防御、低生命、抗性和移动能力是否有真实消费者，不以“有 actor”代替可战斗验收。
- **完成证据：**50／55／80 层下限边界、单怪无伴随、无第二批初始怪物、无普通地面战利品；一次真实环境分配与召唤检查其不同资格；死亡仍能掉落。复用现有 `monster_ecology` 测试，不给每层复制矩阵。

### AR3：「创造神器」卷轴使用闭环（已完成）

实施前核对（来源仍为 `a0d92b6378d148c5262cc236b8fa6ed2ca06a54c`）：

- `artifact_scroll` 使用背包、装备、箭袋／包内物品与脚下地面选择。当前箭袋／背包物品共用 Inventory；目标允许武器、护甲、弹药、首饰、费艾诺及以上光源，以及当前为 Snotling 的蘑菇；排除符文剑、毒针。已知神器／Ego 在选择边界拒绝，未知身份进入原版的无效造物尝试；不允许绕过真实身份和 `NO_REMOVE`。当前没有 `xtra3` 定制物品的独立状态／生产者，不为它预造表示。
- `devices.c` 在尝试前 notice；`cmd6.c` 对 `SV_SCROLL_ARTIFACT` 明确将取消／失败的能量设为零，成功才消耗一张。合法多件目标先缩为一件，剩余部分销毁；工厂返回零价值也视为失败，但保留已发生的造物与 RNG。非法或过期目标在修改库存前拒绝。UI 取消选择不发命令，沿用现有交互；命名留空或取消命名使用工厂随机名。
- 直接调用单次 `create_artifact` 的 SCROLL | GOOD 路径，不经过自然掉落的价值拒绝循环。保留原实例 ID、位置、既有底材属性和诅咒，写入神器名与 `ArtifactCreation` 来源；成功工厂路径鉴定完整属性，独立美德 +2、附魔 +5，卷轴成功再附魔 +1。
- 当前工厂尚未承接弹药的专属 powers 分支及弹药骰面物化，本阶段按同一 `artifact.c` 补足真实消费者。Snotling 已开放且有食用增益，永恒蘑菇必须保留原蘑菇效果、可重复食用与原版 99 回合冷却，并保存恢复；复用现有物品恢复计数。Android 尚未开放，当前不存在装备估值经验消费者，本阶段不以空调用声称完成该种族经验机制。

- 导入 kind 364 的正式物品与中文显示名，沿用 P3 来源身份；调用现有随机神器工厂，补齐“选择已有合法物品 → 生成 → 更新该实例 → 消耗卷轴”的使用路径。
- 对照 `src/spells3.c::artifact_scroll`、`src/devices.c` 和 `src/cmd6.c`，确定可选物品位置、无名武器／防具过滤、神器／Ego／特殊定制／不可移除限制，以及取消、失败和成功的消耗／回合语义；在实现前记录决定，不凭其他卷轴的默认行为套用。
- 明确原版对多件堆叠目标只保留一件的处理、Snotling 蘑菇分支及 Android 经验重算等实际相关消费者。选中物品不通过生成另一件无关联装备来替代；沿用现有实例、背包／装备槽、重量及事务边界。
- 生成属性、名称和来源须持久化；非法目标在真正输入边界拒绝，不能先改库存再因校验失败留下半完成状态。正常生成失败与非法指令分开处理，其 RNG／消耗遵从所核实规则。
- **完成证据：**正式 `demo.item.artifact-creation-scroll` 沿用 P3 kind 364、`70/52` 与权威中文名“创造神器卷轴”，按 `A:91/16` 加入基础池。Rust 投影合法候选，UI 完成选择、堆叠销毁确认和可选命名；更新原实例，不分配替代 ID。五项使用测试覆盖背包／装备保存与后续行动、脚下四件弹药仅留一件并实际射击、非法／过期／已知神器／Ego 与普通失败、零价值造物保留 RNG 和生成结果但返还卷轴／回合，以及 Snotling 永恒蘑菇效果、冷却与恢复。前端验证目标取消不发命令，命名取消／留空使用随机名。

本批物品行为族、随机神器工厂／调度、神器身份、弹药与估值检查的失败项已按真实变化修正并定向复验；内容身份／数量／共享分配、前端交互／事件／本地化、类型检查、相关 Clippy、协议与内容生成物、source/lock 和 26 条 active 契约通过。基础池增加一行；重生成分配审计同时补齐此前已存在的固定神器／光源映射记录，没有额外提升自然分配。State Hash Schema、存档版本与 fixture 未改。本批未构建可玩桌面产物或进行人工试玩；Android 装备经验仍未实现，竞技场正式入口／奖励由 AR4 接入。

### AR4：31 层、守卫、奖励与真实入口（已完成）

正式包 `1.410.0` 接入索引 25、(67,7)、50–80 层以及 691／1110 两名源守卫，无替代组；末层无向下楼梯。来源再次核对为 `a0d92b6378d148c5262cc236b8fa6ed2ca06a54c`，补查 `src/quest.c::_dungeon_boss_death` 的 `final_object` 独立掉落路径。固定奖励沿用 `70/52` 的创造神器卷轴，与普通死亡掉落分开；共享奖励消费者增加已征服状态检查，防止重复处理死亡再次发奖。墙 A 行已完整登记：永久填充／外墙、零权重材质、GRANITE 内墙与两个 NONE streamer；圆形房间不生成独立内墙，零权重与 NONE 无额外生产者。

新增一个核心流程，从实际地表入口击败龙魔像进入 50 层，逐层到达 80 层，保留最终守卫 3–6 HP 等原属性通过真实近战击杀，确认普通掉落、唯一种与一次征服、拾取一张卷轴并改造同一装备实例；随后逐层返回地表、召回末层并再次返回，确认守卫／奖励不再生成。入口、首层、中层、末层、奖励取得／使用后与返回点均验证保存恢复状态哈希。转层部分清除普通怪物和附带临时状态，隔离高层遭遇对路由检查的影响。

该流程与 114 项 generation／monster_ecology／world 既有检查通过；内容 world 62 项、中英文本地化 5 项、相关 Clippy、格式与 source/lock 检查通过。26 条 active 契约逐项观察后仅状态哈希不同：新角色初始化新增竞技场地牢状态，事件、错误种类与其他断言均未变。仅刷新相应哈希并将 baseline 更新为 `contract-v322`，复验通过；State Hash Schema 120、协议 1.247 与存档格式不变，未重生成 Schema／绑定。

- **依赖 AR1–AR3 完成后开放入口。**在正式内容中接入 50–80 层、(67,7) 地表入口与返回、龙魔像入口守卫，以及 80 层唯一种散失金属史莱姆。
- 补全已有来源登记的墙参数与实现说明，沿用地牢稳定 ID 和来源记录。无替代组，不引入新的世界选择开关。
- 最终奖励使用已完成 AR3 的 `70/52` 物品，复用最终守卫奖励发放流程；与普通死亡掉落分开确认，保存／重入／重复处理死亡均不得再发一份。
- 末层无向下楼梯；保存恢复、楼层往返、召回和地表返回沿现有流程。中文名逐字采用本次 source ref；实施时更新正式包版本与 lock，不预留版本号。
- **完成证据：**由真实地表入口进入首层，代表中层转移、末层战斗与奖励、卷轴实际使用、返回地表和保存恢复闭环均通过；不是只靠直接构造末层状态证明入口已接通。

### AR5：聚焦验收与交付（已完成）

- **内容：**再次从 `master` Git 对象核对索引 25、(67,7)、50–80 层、691／1110 守卫、`70/52` 奖励、L/A 参数及中文名，来源仍为 `a0d92b6378d148c5262cc236b8fa6ed2ca06a54c`。复用 AR4 的正式绑定／31 层链检查；本批 source/lock 验证通过，包版本与 hash 未变。
- **生成与规则：**新增一项正式楼层验收，50／65／80 层各三个种子，共九个场景，全部可通行区域连通，保留普通门，每个场景均通过真实移动指令触发生成的陷阱。复用 AR4 同一规则和内容下的 114 项 generation／monster_ecology／world 及完整入口流程证据，包括永久墙不可挖、每房间单怪、普通死亡掉落、全层往返和一次性奖励。补跑守卫免疫、近战／箭矢减伤、死亡掉落与移动地形约束；相关场景统一使用 `arena_dungeon_` 名称。
- **物品与界面：**本批 106 项核心检查通过，覆盖正式代表层、items、artifact_identity、随机神器工厂／调度、普通怪物开门及上述守卫专项；卷轴五项用例保留装备实例、堆叠销毁与实际射击、失败／非法目标边界、Snotling 冷却和保存恢复。两项定向 UI 检查验证成功／失败提示、核心候选、堆叠确认以及目标／命名取消。
- **类型与契约：**本批只补测试和文档，没有规则、内容、类型、DTO、初始化或 RNG 改动；复用 AR4 的 62 项内容 world、26 条 active 契约及本地化证据，不刷新 fixture 或重生成 Schema／绑定。相关 Clippy、格式、source/lock 与文档链接检查通过。
- **交付：**`docs/status.md` 已收口为 AR1–AR5 完成。保留 96×33、六房间、面积预算 800、既有通道／单门与陷阱适配，不宣称逐格或 RNG 序列复刻。真实入口和核心自动闭环已验收；本批未构建可玩 EXE，未执行桌面 E2E、Android 或人工试玩。

## 5. 范围与完成标准

本批不接入城镇竞技场赛程、随机任务系统、奥林匹斯山或安格班，不重做随机神器工厂，不维护旧开发存档兼容。测试从新存档开始。共享物品使用路径若与其他工作树出现实际并发重叠，再协调文件与提交；已有共享机制不另造副本。

完成需同时具备：**圆形房间及有效通道、专用初始与运行期生态、可使用的创造神器卷轴、真实地表入口与 31 层链、最终守卫奖励不重复，以及按影响范围通过的自动检查**。只增加地牢 JSON 或获得一次构建成功均不算完成。
