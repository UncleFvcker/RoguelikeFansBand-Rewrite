# 反魔洞穴／反近战洞穴生成与接入计划

状态：A1–A3 已完成无最终守卫支持、反魔规则与分类、反近战规则与生态；A4–A5 尚未实施，16/17 尚未进入正式 world 或开放入口。目标为两座各 40–50 层、11 层，共 22 个楼层定义，每个新角色只开放替代组选中的一座。

## 1. 目标与基线

本批交付原版 16/17 替代组：反魔洞穴禁用相应施法、保留天生怪物能力；反近战洞穴阻止玩家和怪物近战、保留远程行为。两者只有入口守卫，没有最终首领或固定征服奖励。到达 50 层后正常返回与召回即构成探索闭环，不补造首领、神器或通关属性奖励。

- 工作树：`D:/codex/RoguelikeFansBand-Rewrite-dungeons-towns`，分支 `codex/dungeons-towns`，规划基线 `8239bce30`，正式包 `1.401.6`。当前世界含 26 个地牢条目，其中包括既有替代分支。
- 来源：`D:/codex/Frogcomposband/master` 的 `master` Git ref，实际提交 `a0d92b6378d148c5262cc236b8fa6ed2ca06a54c`；本次通过 `git show` / `git grep` 读取 Git 对象。
- 核对时 main 为 `b374f4344`，本方向尚未同步。最新主线已有心灵术士相关工作；实施时按实际合入状态覆盖其施法入口，不把当前分支六职业当作永久边界。本计划不执行合并或修改其他工作树。
- 规划时，本树、main 和法术道具分支尚未找到本组正式地牢 ID 或地牢级禁魔/禁近战实现。既有反魔装备/状态不能直接替代地牢规则。

| 候选 | 当前判断 |
| --- | --- |
| **反魔洞穴／反近战洞穴** | 推荐下一批；两名入口守卫和主要地形能力已有，缺无最终守卫模型、战斗限制与生态筛选。按原版替代组一起接入，避免同时开放两座。 |
| 黑暗洞穴／灾难区 | 后续替代组；还需魔法黑暗与废水的实际消费者，不能只改地图颜色。 |
| 竞技场地牢 | 保留后续；地牢 25 的隔离房间、中心怪物和生态特判与普通 `ARENA` 标记不同。 |

## 2. 已核实来源

| 项目 | 反魔洞穴 | 反近战洞穴 |
| --- | --- | --- |
| 索引与英文身份 | `N:16:Anti-magic cave` | `N:17:Anti-melee cave` |
| 正式中文显示名 | **反魔洞穴** | **反近战洞穴** |
| 世界坐标 | `P:6:84` → **(84,6)** | `P:45:47` → **(47,45)** |
| 深度 | 40–50 | 40–50 |
| 地板与墙 | FLOOR 100%；GRANITE 100%；岩浆矿脉与石英矿脉 | 同左 |
| 通道参数 | `tunnelPercent=50` | 同左 |
| 共用生成标记 | `CAVERN / WATER_RIVER / LAVA_RIVER / ARENA / DESTROY / LAKE_WATER / LAKE_LAVA / LAKE_RUBBLE / LAKE_TREE` | 同左，另有 `GLASS_ROOM` |
| 生态与限制 | `MONSTER_DIV_0 / NO_MAGIC`，无 M 行偏好 | `MONSTER_DIV_0 / NO_MELEE`，无 M 行偏好 |
| 入口守卫 | 672 `Juggernaut of Khorne`，**恐虐钢牛**，等级 43 | 896 `Bazooker`，**火箭筒兵**，等级 35 |
| 最终守卫与奖励 | 无 | 无 |
| 替代关系 | 声明 `SUBSTITUTE_17` | 由 16 引用，不反向重复声明 |

身份、参数与标记来自 `lib/edit/d_info.txt`；中文显示名采用 `src/dungeon_name_zh.inc`，守卫名采用 `src/monster_name_zh.inc`，属性来自 `r_info.txt`。帮助文本也写过“反魔法洞穴”，这里采用实际显示名称表的“反魔洞穴”，不自行合并译名。两座均未声明 `BIG / ALL_SHAFTS / NO_DOORS / CAVE`；不从其他洞穴复制这些标记。W 行的 30 不直接解释为新增 30 级入场门槛。

### 生成标记的实际执行边界

`src/generate.c::gen_caverns_and_lakes` 对水湖要求深度 **>50**、熔岩湖 **>80**；本组深度内不能因声明了标记就安排这两类湖。碎石类要求 >35、树木类要求 >5，可以出现。`DESTROY`、湖泊与普通洞窟在源选择流程中有互斥；`ARENA` 还受生成选项和概率控制，不是每层固定空房。

同文件的河流选择先抽取 1/7，再要求 `randint1(depth)>5`；水/熔岩选择使用 `MAX_DEPTH=128`，无其他模式干预时，熔岩分支概率为 `(depth+1)/256`。源不会在碎石/树木湖层叠加水河或熔岩河。生成计划需保留这些来源事实，再明确当前引擎的适配，不能把“源声明”当作“该深度实际发生”。

反近战的 `GLASS_ROOM` 也不能直接当作强制玻璃房间：本次检索该 ref 的全部 src，除定义/解析外只找到 `rooms.c:4554` 的空条件分支。房间模板另可包含玻璃地形，但不由这个空分支强制生成。本批在来源计划保留该标记，不新增专门玻璃层、玻璃门或局部玻璃房间机制。

### 战斗与生态的实际执行边界

- `src/dungeon.c` 的 `m` 入口按职业/形态处理 `NO_MAGIC`；书本施法与心灵术士等魔法职业会被阻止，不能只检查 `AbilitySourceDto::Learned`。源列有狂战士等特殊技艺例外，本批只映射实施时已经支持的实际职业/形态，不新增这些职业。种族/变异能力以及卷轴、装置、装备激活不能因为最终也调用 effect 就一律封禁；个别近战效果另按来源处理。
- `src/monspell.c::mon_spell_cast` 对敌对怪物先正常选能力，再拒绝非 `MSF_INNATE` 的结果并回到普通行动；不能预先清空魔法候选，变相提高吐息等天生能力频率。怪物对怪物的分支另有 AI 过滤和 `_spell_fail`，包括 `STUPID` 的差异，不能假定两种目标共用同一拒绝时机。
- `src/monster2.c::restrict_monster_to_dungeon`：反魔要求 `mon_race_has_innate_spell`，`MON_CHAMELEON` 例外；反近战要求 `mon_race_has_attack_spell`，后者仅指 BREATH/BALL/BOLT/BEAM/CURSE 类型，不等于“有任意能力”。反近战的变色龙例外与 `summon_specific_who != -1 || !allow_pets` 条件均须核对实际召唤调用。入口守卫是固定地表实例，不经过洞内普通生态白名单；恐虐钢牛没有天生法术也不能因此消失。
- `src/cmd1.c::py_attack`、`melee1.c`、`melee2.c` 阻止玩家、怪物对玩家及怪物互殴。源 `devices.c EFFECT_STUNNING_KICK`、`spells_s.c` 等还在特殊近战效果入口检查；仅拦移动撞怪会漏掉这些消费者。远程攻击不因相邻目标而被归成近战。

## 3. 当前缺口与最小实现方向

1. **无最终守卫表达缺口（A1 已解决）。** 规划时 `DungeonDefinition.guardian_actor_kind_id` 和导入器 `DemoWildernessDungeonPlan.guardian` 都是必填，校验要求所有叶子层都有 guardian。现已改为可选，保留层链连通、叶子/末层一致和已有守卫引用完整性，不使用空字符串或入口守卫充当最终守卫。
2. **缺地牢级限制。** 在现有 DungeonDefinition 上增加本批必要的限制字段，核心根据当前楼层所属地牢读取；不另存玩家状态、不创建前端规则或新的规则管理器，也不把地牢禁魔写进装备 AntiMagic 被动以免影响豁免等其他语义。
3. **怪物能力分类不完整。** 现有 `monster_ability_is_innate` 为减伤/闪避识别吐息和少数特殊伤害形状；它不是完整 `MSF_INNATE`，例如 SHRIEK、射箭、狂暴等不能靠伤害骰或“是否物理伤害”判定。优先在已有怪物能力候选记录保留源天生/攻击法术分类，并由导入器从真实 token/类别给出，供施法限制与分配读取。保持已有能力 ID；注意按伤害形状去重的 ability 可能丢失源身份，不能随意给共享 ability 打标签导致其他来源一起改变。
4. **生成底座足够复用。** 现有房间、洞窟、碎石/树木地形、破坏区、普通楼梯及河流 alternative 已有表达。采用下面的代表层适配，不为本批重建完整原版随机地图生成器。

## 4. 分批实施

### A1：支持无最终守卫的地牢

**已完成（2026-09-10）**：正式地牢和来源计划的最终守卫均可省略；有声明时仍校验怪物引用及每个末层守卫绑定，无声明时拒绝末层 guardian/守卫奖励。读档不再假定末层必有首领，并拒绝无首领地牢伪造 `guardian_defeated=true`。生成、入口守卫死亡、返回与召回复用既有路径，没有新增持久状态。

验证：新增两层最小内容场景与运行时场景，覆盖末层标记、缺失/错配/未声明守卫拒绝、入口守卫死亡、无首领终点、保存哈希往返、伪造征服状态拒绝、返回与召回。104 项核心（world、monster_ecology）、9 项内容和 26 项导入计划测试通过，既有首领征服回归在核心范围内。临时 selection 加入原版 16 的无最终守卫计划后来源审计通过；虚构奖励及漏写原有首领的反例被拒绝，临时结果未回写正式包。来源提交仍为 `a0d92b6378d148c5262cc236b8fa6ed2ca06a54c`。

已生成并检查 world 内容 Schema，core/content/importer Clippy、格式与正式内容锁检查通过。正式内容未变，包/lock 保持 1.401.6，hash 仍为 `75fc71950cce670cecab5510da36552bc94333cd0b9a627126cfe85511da9fd6`。未改变协议、保存格式或 State Hash Schema，未刷新契约；本批没有开放洞穴或制作可玩构建。

- 将正式地牢与来源计划的最终守卫声明改为可选，更新实际校验及守卫排除消费者。有守卫的既有地牢仍必须满足原有末层绑定；没有守卫的地牢不允许偷偷声明末层守卫或守卫奖励。
- `finalFloor` 继续表达层链终点，50 层没有向下楼梯。终点不自动写入 `conquered`，入口守卫击杀也不触发最终奖励。复用已有入口守卫状态、深度记录、返回与召回。
- 覆盖一座最小无守卫楼层链，以及既有有守卫地牢的引用错误拒绝和正常征服回归；不为 26 个旧地牢复制新测试。

**完成标准：**合法无首领地牢可生成、保存和返回，既有首领与奖励校验没有被放松。本批底座完成前不把 16/17 写成两个假首领地牢。

### A2：反魔规则与能力分类

**已完成（2026-09-10）**：`DungeonDefinition.noMagic`、玩家零耗时拒绝/执行/可用性投影及怪物分配与施法已接通。当前分支六职业中，狙击手的 16 个 `m` 技艺按源禁用，包括专注和特殊射击；`U` 探查保留。其余已支持职业的独立能力、种族/变异能力、卷轴、装置与装备激活保留各自入口语义。合并新增职业时，须按真实 `m`/`U` 来源补 `blockedByDungeonAntiMagic`，不能仅凭职业、伤害或资源类型判断。

来源仍为 `a0d92b6378d148c5262cc236b8fa6ed2ca06a54c`。[逐候选审计](../packs/rfb-demo-original/legacy-monster-spell-classification.json)覆盖 1,386 个正式来源怪物和 7 个既有自定义施法样例：985 个来源天生施法候选、512 个来源天生分配资格、772 个来源攻击能力资格、75 个 STUPID。41 个只有附身用天生能力的怪物（如雪人的 BERSERK）仍有分配资格，所以分配标签与实际施法候选分别记录。未知源分类拒绝导入；原版 GAZE 属攻击法术但不是天生能力。884 个 actor 文件的语义差异逐一核对，仅增加分类和 STUPID，没有重导数值、掉落或能力列表。同步移除已实现的 STUPID/NO_STUN 遗留遗漏声明并修正其数组尾逗号，正式 1,352 项 selection 可再次完整审计。

自然分配与护卫显式接收目标楼层，生成时不误用出发楼层；后续补怪、玩家/物品召唤分类、怪物召唤分类及复活候选共用禁魔资格检查，保留原版变色龙和直接指定召唤的语境。怪物对玩家保持原频率和候选抽取，抽中魔法后返回普通行动，不重抽、不设置施法冷却；怪物互施时按 NO_MAGIC 的 AI 预滤及 STUPID 失败例外处理，自身增益按敌对目标语境判断，变形读取当前外观的候选与 STUPID。既有魔法减伤和闪避消费者未改写。

验证：175 项核心检查通过（6 项禁魔专项、world/monster_ecology/monster_ai、书本与召唤/物品能力、狙击手、原有天生伤害闪避及卷轴/魔杖/装备激活对照），146 项内容检查、187 项 importer 检查通过；内容全检发现并补正了既有拉莱耶入口快照遗漏。26 条 active 契约全部通过，无刷新。core/content/importer 的 lib/tests Clippy、内容 Schema 检查和正式锁校验通过。包版本为 **1.401.7**，hash 为 `4f7164cab08201d2957920aac1f3b45865d4633f25ea0046f81d74ef68e79f21`；未改协议、保存格式或 State Hash Schema。真实楼层进入、保存、返回和召回在测试内容中验证禁魔上下文；正式 world 尚未启用 16/17，没有制作可玩构建或宣称人工试玩。

- 接入地牢禁魔字段和源能力分类；来源审计列出受影响的正式候选，定向更新，避免重导其他内容。普通怪物分配、后续补怪及召唤分类共用已有候选路径，在真实调用边界保留召唤语境。
- 玩家执行与可用性投影消费同一规则：书本法术和已支持的魔法职业技能被拒绝，已支持的非魔法技艺按源例外保留。种族/变异、卷轴、装置与装备激活选最小对照验证，不把资源类型或 ability source 当作唯一魔法判断。
- 怪物对玩家按正常频率/候选抽取后拒绝魔法，保留普通行动；怪物互相施法按源 AI/失败语义处理，明确实际支持的 STUPID、友军与变形消费者。拒绝不能扣掉本不该消耗的玩家资源，也不能重新抽取直到获得天生能力。
- 天生/攻击分类只补当前缺口。若复用分类会改变既有魔法减伤或闪避行为，单独解释影响并验证，不顺带把旧消费者全部改写。

**完成标准：**真实地牢上下文中，禁魔执行、投影与怪物分配一致；吐息/火箭等应保留行为正常，纯魔法候选被拒绝，离开地牢后恢复。存在不明源分类时先核实，不以默认标签掩盖。

### A3：反近战规则与生态

**已完成（2026-09-10）**：`DungeonDefinition.noMelee` 接入当前楼层判断、玩家/怪物/友军/宠物近战与反击。拦截发生在命中、伤害、熟练度和接触效果之前；普通主动攻击仍花费回合。普通怪物会继续寻找可移动位置，STUPID 或混乱怪物会浪费攻击行动；同时具备 KILL_BODY/MOVE_BODY 的怪物不会把被禁止的攻击变成推挤。

来源仍为 `master` 的 `a0d92b6378d148c5262cc236b8fa6ed2ca06a54c`，本批核对 `cmd1.c::py_attack`、`melee1.c::make_attack_normal`、`melee2.c::mon_attack_mon/process_monster`、`gf.c::GF_ATTACK` 以及特殊能力外层调用。GAZE 虽属于攻击法术，但通过 `GF_ATTACK` 调用普通近战，所以保留正常施法抽取/冷却而不产生近战效果。奇妙狂乱、龙之打击和惊恐打击保留施放费用；惊恐打击仍尝试逃脱，骑乘冲锋保留移动和装置消耗。吸血的种族/变异能力按 `spells.c::do_cmd_power` 先判定施放失败：失败照常消耗，成功后才被 NO_MELEE 取消且不耗时、不付费、不递减冷却；取消仍保留那一次施放 RNG。可用性投影显示吸血不可用，直接命令仍按上述顺序执行。

自然生成、补怪与怪物召唤使用 A2 已审计的 `attack-spell` 分配标签，治疗、加速和召唤本身不算攻击；变色龙例外保留。`monster2.c::restrict_monster_to_dungeon` 的玩家例外按 `SUMMON_WHO_PLAYER` 与原版 `allow_pets` 判定，本树宠物功能启用。玩家法术和物品召唤即使产生敌对结果也保留原候选池；周期吸引、混沌赏赐、诅咒和怪物施法按各自真实来源传递语境。宠物自身施法仍属怪物召唤。该例外不绕过 NO_MAGIC 的天生资格，固定指定身份的既有召唤入口保持原边界。

验证：476 项核心回归通过，包含 8 项禁近战专项以及既有战斗、怪物 AI/移动/生态、玩家施法、特殊物品、楼层进入/保存/返回/召回；最后的 KILL_BODY/MOVE_BODY 修正另以 14 项调度/移动检查通过。58 项内容、39 项本地化和 26 条 active 契约通过；无契约刷新。射击、投掷、玩家法术、吐息/火箭与正常楼层对照一致；投掷伤害对照使用现有 ThrowProfile 的最小测试定义，本批未扩展正式投掷内容。内容 Schema 已生成并检查，正式内容未变，包/lock 校验保持 **1.401.7**，hash 为 `4f7164cab08201d2957920aac1f3b45865d4633f25ea0046f81d74ef68e79f21`。未改变协议、保存格式或 State Hash Schema，未制作可玩构建或宣称人工试玩。

core/content 的 Clippy、格式检查与 `git diff --check` 均通过。

- 接入地牢禁近战字段；覆盖 `resolve_player_melee`、怪物近战、玩家召唤物/宠物近战和已存在的特殊近战入口，修正在实际调度层应当拒绝的尝试。普通移动、射击、投掷、施法及远程伤害照常工作。
- 检查本树 `monster_ai.rs` 的凝视能力会复用 `resolve_monster_melee_target` 的现状，按源实际凝视/攻击语义判定，不能仅凭函数名一刀切。源主动近战拒绝与法术不可用的回合处理不同；核定能量/回合、命中 RNG、消耗和反击，禁止把所有拒绝强制改成免费动作。
- 自然生态使用“有攻击法术”筛选；只有治疗/加速/召唤的怪物不因此算攻击施法怪物。玩家允许宠物召唤的源例外须在当前已有召唤调用中保留，不能借地牢过滤悄悄提升宠物候选。

**完成标准：**玩家、敌人、友军近战均无伤害或附带命中效果，远程行为正常；攻击法术筛选与天生分类分别生效，不能用一份“会施法”白名单替代两座洞穴的规则。

### A4：接入替代组、22 层和真实入口

拟用稳定 ID：`demo.dungeon.anti-magic-cave` / `demo.dungeon.anti-melee-cave`；楼层为 `demo.floor.anti-magic-cave-depth-40` 至 `-50` 与对应 anti-melee ID，入口和生态表沿用相同命名模式。实施前再次查重。

1. 在 `legacy-wilderness-selection.json` 登记两个位置和完整来源计划，16 指向 17。来源同步审计写临时 world 副本，正式 world 仅追加本批内容，保留其他地点。
2. 复用现有种子化替代选择，只显示所选地点自己的原版坐标、入口与地表守卫。源两座坐标不同，不把替代入口都搬到 (84,6)。另一座不能通过地图、旧目标或召回进入。保存恢复不重选。
3. 源替代函数对旧 `seed_dungeon=0` 和 melee/no-melee challenge 有特殊分支。本项目开发从新档开始，沿用当前 seed 选择，不增加旧档兼容或未支持的挑战模式；明确该适配，测试两个正常种子，不把源旧档特殊值当作本项目新档默认双开。
4. 入口守卫复用 `demo.actor.juggernaut-of-khorne` / `demo.actor.bazooker`，使用现有实际局部入口定位，安排在合法相邻格；生成、死亡、重访、读档和地表返回保留实例状态。无需新增 actor 或物品定义。
5. 各 11 层使用下述生成方案，40 层可从真实地表入口进入，50 层是无首领终点。生态走已有全局分配，`specialDiv=0` 不等于空怪物池；限制字段按实际候选能力过滤，继续遵守深度、稀有度、唯一怪、归属与其他首领排除。

**生成适配方案：**

- 基础采用 66×22、普通房间/通道、地板与花岗岩、岩浆/石英矿脉；不套用拉莱耶混合水地板预算。普通楼梯沿用现有数量表达，根层保留返回入口，50 层只有向上通路。
- 默认普通布局；拟在 42 层表达洞窟、45 层表达 ARENA 开阔布局、46 层表达碎石类湖区、48 层表达树木类湖区、49 层表达破坏区。其余层保留普通房间。具体面积沿用已验证预算并按本批生成抽查调整，不预先堆满所有地形。
- 普通/洞窟/开阔/破坏层按现有 `river.chanceOneIn=7`，water 为主、lava alternative 的分子为 `depth+1`、分母为 256；46/48 层不配置河流。当前表达省去源 `randint1(depth)>5` 的第二次门控，实际河流略多，此差异须在交付记录中保留，不宣称 RNG 或频率逐次复刻。
- 40–50 层不安排水湖、熔岩湖。水与熔岩按既有通行、视线、投射和承物规则执行，不能用 walkable 代替所有判断；普通门不会因 `GLASS_ROOM` 变成玻璃门。
- 代表层是当前生成器的明确适配，并非原版每层概率抽取 ARENA/洞窟/湖泊/破坏区。无需新增通用布局变体框架；若抽查发现真实生成缺陷，只修本批实际使用的既有职责。

**完成标准：**两个种子分别选中两座洞穴，从各自真实局部入口到 50 层并返回原位置；入口守卫不重复，无最终首领/奖励，规则只在所属地牢生效。A1–A3 尚未到位时不开放入口。

### A5：聚焦验收与交付

- 来源/内容：核对 16/17、(84,6)/(47,45)、深度链、正式中文名、入口守卫、替代抑制、无最终奖励、source 与 lock。
- 生成：先检查两种替代结果的根层和末层，再检查代表性洞窟、河流两种材质、开阔、碎石/树木与破坏场景。检查玩家/守卫/普通怪物与物品落点、普通楼梯、终点无向下通路和地形预算；复用已有各生成能力测试，不为 22 层逐层复制场景。失败揭示更广影响时再扩展。
- 行为：复用 `game::tests::world`、`monster_ecology`、`monster_ai`、战斗、能力施放/召唤及物品测试；新测试前缀用 `anti_magic_cave_` / `anti_melee_cave_`。补源分类正反例、禁用后的实际资源/回合和离开恢复，不只断言 JSON 字段。
- 保存与返回：覆盖替代选中状态、入口守卫死亡、底层返回/召回及限制恢复。普通地牢的战斗/施法、有首领征服和拉莱耶水上投射是直接回归对象，按实际改动复用有效证据。
- 类型与契约：A1/限制/分类的内容类型变化生成对应 Schema；只有 DTO 变化才生成协议绑定。新增地牢状态及公共候选/RNG 若影响初始化或哈希，先观察差异、执行 active `verify-all`，解释后刷新；不因 contentHash 单独升级 State Hash Schema 或刷新全部 fixture。
- 内容变化按实施时正式版本更新 pack/lock，并更新 `docs/status.md`；本计划不预留版本号。完成说明区分定义、规则、入口与自动验收。可玩 EXE 仍须另做 Tauri standalone，不默认执行全量前端、Android、桌面 E2E 或路径可达性专项。

## 5. 修改位置与协作边界

主要修改现有 `crates/rfb-content/src/definitions/{worlds,actors}.rs`、`validation/worlds.rs`、`crates/rfb-legacy-import/src/content.rs`，核心的 `game/world/`、`monster_ecology.rs`、`monster_ai.rs`、`monster_combat.rs`、`player_combat.rs`、`abilities/casting.rs` 及实际投影/召唤消费者。规则放回相应职责，不能继续堆入 `game/mod.rs`；已有 mod.rs 调用点只做必要接线。

内容进入正式 world、selection、入口地形、生态表和两套 locale，守卫沿用已有 ID。同步 main 后若当前职业增加，A2/A3 验证范围跟随实际入口；只有真实共享机制或并发写入重叠才协调，不把所有共享文件设为审批点。

执行依据：[内容开发](../docs/content-development.md)、[验证与契约](../docs/testing.md)、[并行协作](../docs/parallel-development.md)。后续按 A4–A5 推进；A1–A3 已交付规则底座，洞穴入口仍留给 A4。
