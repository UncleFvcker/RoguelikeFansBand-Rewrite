{
  "name": "rfb-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "license": "MPL-2.0",
  "scripts": {
    "wasm:build": "wasm-pack build ../crates/rfb-wasm --target web --out-dir ../../web/src/wasm --out-name rfb_wasm",
    "dev": "npm run wasm:build && vite",
    "typecheck": "tsc --noEmit",
    "build": "npm run wasm:build && npm run typecheck && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@msgpack/msgpack": "3.1.3",
    "pixi.js": "8.19.0"
  },
  "devDependencies": {
    "typescript": "7.0.2",
    "vite": "8.1.4"
  }
}

