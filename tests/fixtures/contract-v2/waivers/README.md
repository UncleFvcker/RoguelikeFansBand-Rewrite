# Contract diff waivers

This directory contains only explicitly approved, fixture-scoped behavior differences.

Each waiver must be a JSON file named exactly after its lowercase `id`, for example
`waiver-2026-001.json`. Wildcard fixture or assertion scopes are forbidden.

Required fields are defined in `design/baseline-update-policy.md`. There are currently no
approved differences.
