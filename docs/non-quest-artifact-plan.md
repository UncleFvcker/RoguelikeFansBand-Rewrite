# 非 QUESTITEM 固定神器推进计划

## 范围与基线

本计划接续Q1–Q5，处理原版固定神器中**没有`QUESTITEM`标志**的成员，不包含普通底材全量补齐、Ego、随机神器或剩余13件QUESTITEM。当前工作树`codex/realms-items`，本批基线`65eef6fb3`。N1全部65件已验收，N2的47件已接入普通／instant生成及激活；未闭合源规则见下文。正式包／lock均为1.472.0。2026-09-13重新读取`D:/codex/Frogcomposband/master`的`master` Git对象，实际提交`a0d92b6378d148c5262cc236b8fa6ed2ca06a54c`。

| 范围 | 原版记录 | 已定义 | 未定义 |
| --- | ---: | ---: | ---: |
| 非QUESTITEM固定神器 | 309 | 244 | 65 |
| QUESTITEM固定神器 | 83 | 70 | 13 |
| 全部固定神器 | 392 | 314 | 78 |

初始177件中，158件已有正式底材，19件还缺底材。N1、N2共补入112件定义后，当前未定义65件，其中46件已有底材、19件缺底材。已有定义只说明内容存在，不能代替规则、普通生成、额外命名掉落或实际验收。

本次纠正逐项清单漏记的198“逃亡者卡姆依的”和231“荆轲的”，并按正式目录更新已有底材引用；完整名册恢复为392行。全部未定义神器的缺底材数按当前目录为26（非QUESTITEM19、QUESTITEM7），取代旧摘要27。中文名逐字来自源中文表；下文无特别说明的数字均为artifact source ID。

## 排期与派工粒度

N1–N7是**互斥的候选阶段**，初始合计177件（含N1已加入定义的65件），按本次已看到的主要依赖分组；不是177件都已完成规则审查。每个实际小组通常2–3件，有独有消费者时只做1件。实现前重新核对源构造、装备、战斗、激活及获取调用者；发现遗漏依赖就调整该件归属并保留记录，不能删源行为来凑完成数。

| 阶段 | 数量 | 交付重点与首批 |
| --- | ---: | --- |
| N1 普通装备（已验收） | 65 | 已有底材，以无显式激活为主，优先复用装备／诅咒／普通神器生成；198在N1c发现漏记传送激活，本组一并接入。65件及统一验收完成；补齐加强锋锐、生成随机诅咒、弓弩品牌／杀伤和无弹药预览消费者。 |
| N2 通用激活（入口已接入，源规则未完全闭合） | 47 | 全部普通／instant生成与装备激活已接入，核对源参数、目标、增强、取消顺序和冷却保存；8／93／122怪物最大HP削减、84／128跨层确认仍开放，验收范围见下文。 |
| N3 生成及战斗特例 | 25 | 先139／334祝福概率、60生成门槛；再171／172双刀、125／153弓箭配对。其余按单个特例关闭，不先造通用套装系统。 |
| N4 专属激活及可变实例 | 15 | 先199血月首次构造／激活重抽，再144村正、255如意金箍棒；其他召唤、复合效果逐一核对已有消费者，能复用者直接复用。 |
| N5 领域石 | 10 | 先290／282，再283／284／286／288／289／297；285／287保留真实领域入口条件。专用底材、instant生成、光源及领域收益一起交付。 |
| N6 其他专用底材 | 6 | 先7／364；16／273作为特殊发射器小组，151／389再分别处理盾牌和宝石。不得把INSTA_ART底材塞进普通底材分配。 |
| N7 身份与条件分支 | 9 | 先138／355现有职业交互、146现有吸血鬼／装备身份链；233／354等待真实性别状态。215、245／246、395区分普通分支和未开放身份加成。 |

为配合大阶段验收，推荐**先完成整个N1的65件及统一验证，再进入N2**；N1a–N1d只是首轮12件，剩余53件现已一并加入。其余阶段同样按小组实现、在大阶段结束时编译验证。已有定义的额外命名获取沿下文单独补齐，不占初始177件名额。

## 第一轮：明确可派工的小组

| 小组 | 成员 | 核心行为与关闭条件 |
| --- | --- | --- |
| N1a | 6矮人项链、220格格的、55科温的 | 首件经完整普通池实际生成、拾取、未知保存、鉴定与装备；生命上限／再生、照明、属性维持分别核对。其余两件保留真实底材与神器门槛抽样。 |
| N1b | 43暗影杰克的、168吉尔斯的、206神父的 | 潜行／心灵感应、负潜行与抗性、反射／照明；卸装撤销和保存续演。43的额外命名掉落另记状态。 |
| N1c | 103石手巴林的、198逃亡者卡姆依的、231荆轲的 | 正式战斧／忍者刀／短刀底材，实际近战、品牌／杀伤、额外攻次与锋锐；198另补漏记的TELEPORT:10:25及冷却保存。不把装备面板数值当作战斗验收。 |
| N1d | 234义经的、360『黑色贝雷帽』、392『笨蛋帽』 | 感知／警告、时间与常规抗性、重诅咒和负法术容量；至少用真实施法职业核对容量影响。 |
| N2a | 84『维尔温德尔』、128『草薙剑』 | `item_use.rs`已有Escape四类分支；普通生成、真实激活、失败／取消、分支限制、冷却保存。品牌与随机附加逐件核源，额外掉落不能由普通生成测试代替。 |
| N2b | 4卡拉马斯之、127希姆林之、209『隐者之紫』 | Protection from Evil实际目标／等级条件，分别保留源强度、时长、冷却与装备被动。 |
| N2c | 8『弗拉基尔』、93希优顿的、122『图尔米尔』 | Drain Life的有效生命目标、抵抗／免疫、源伤害及回充，不用普通物理伤害替代。 |
| N2d | 10纳雅、11南雅、12维雅 | 三种元素球与戒指本身属性；方向／位置目标、免疫对照、冷却中保存、失败／取消时费用。完整显示名从逐项清单取源中文字符串。 |
| N3a | 139『温不倒』、334『微软 Edge』 | 核`spells3.c`祝福成功概率10与普通诅咒流程，按真实调用者接入，不误当作只需静态负面flag。 |
| N3b | 60费诺的 | `object2.c`普通固定神器候选额外1/3门槛；同时保留速度激活和普通稀有度，测试拒绝时RNG与唯一性。 |
| N3c | 171武藏的、172武藏的 | `xtra1.c`双持组合及手位，单件／双件／反向手位／卸装后的实际攻击率和保存。 |
| N3d | 125巴德的、153巴德的 | 补153专用底材179，核`cmd2.c`真实弓箭组合。复用Q5神器弹药不破碎与落地链；不顺带赋予梵天法宝的返回属性。 |
| N4a | 199『血月』 | kind108已存在。首次构造与激活共用源重抽规则，清除旧属性与相应知识，保存实际属性和RNG；重新鉴定与装备消费者一致。 |
| N5a | 290工匠之石、282自然之石 | 补kind620／612；先追`easy_realm1`实际费用／失败率消费者，再接石头装备与激活。测试领域匹配、不匹配、切换、卸装和保存。 |
| N6a | 7爱迪生的、364全视之眼 | 补kind604／627并核instant路径；燃料／光源参数按源，364另有`xtra1.c`眼球照明特例，不能仅复制Q3九头蛇之眼。 |

“推进N1a”表示前三件；“推进N1”表示整个65件阶段。N1已完成全部65件及统一验证；本次“推进N2”覆盖整个47件阶段。N2遗留源规则单列，后续新定义阶段为N3，不自动展开。

## N1 实现与验收范围

首轮12件保留已有实现：项链照明+1、SEARCH按5*pval、荆轲额外150百分之一攻次，234独立`yositsune-helm`保留235义经腹卷；392重诅咒、INT/WIS-3及负法术容量由真实Mage对照。其余53件使用已有正式底材、源W/P/pval、权威中文名和普通神器门槛；原始记录保存在`n1-remaining-artifacts.txt`，逐项身份仍以JSON清单为准。

本阶段补齐四个实际规则缺口：81 Aglarang的`VORPAL2`进入已有加强锋锐消费者；58与352的随机诅咒在普通固定神器构造后按`object2.c::apply_magic`调用既有`get_curse`并保存实例结果；弓弩与弹药按`object1.c::missile_flags`合并品牌／杀伤，保留较强等级，弓弩属性不再串入近战。无弹药时的射击预览只取普通弹药，避免新神器David污染默认命中与伤害。没有新增协议或存档字段。

测试覆盖真实底材／稀有度生成、未知拾取保存、鉴定装备、实际近战／射击、卸装与永久诅咒拒绝、唯一性和保存后RNG续演。贝露西尔／卡谢伊使用真实Mage学习并施放Arcane Zap，对照只抵消法术威力的实例；特尔弩与特尔弩矢检查强杀伤不被降级；神器弹药保留不破碎／落地，未赋予梵天法宝的返回属性。既有锋锐测试加入Aglarang，反射测试加入Mr. Shine。完整普通池由N1a首件贯通，其他成员控制真实底材和深度；受控XP、HP和种子不代表自然获取、练级或通关。

`REGEN`沿用现有每10 tick回复1 HP适配，与源100%再生率不同。198源`TELEPORT:10:25`保留在N1，复用距离100、难度10和250 tick冷却；现有候选选择／道具耗时与源150%行动耗时、Astral Guide调整及未开放Troika钩子存在差异。额外命名掉落继续独立排队，包括43／55／360以及本轮71／72／154／176／221／323；普通生成通过不表示额外掉落已接入。

统一验收已完成：核心1834项有效覆盖（首轮1829通过，5项修复后定向复验；另9项ignored），内容177项（2项旧断言修复后复验）、本地化39项、N1源参数5项、C估值298例／482个正式实例、103个Build／160个Ego来源审计、26条未刷新契约，以及相关Clippy与格式检查通过。桌面脚本：`node web/e2e/ordinary-equipment-standalone.e2e.mjs --non-quest-n1`；证据目录`test-results/non-quest-n1/`。四个代表为矮人项链、Aglarang、Hellfire与笨蛋帽，来自正常Mage新档和显式场景准备；14次UI操作全部逐步原生保存／恢复并核对哈希。两件武器实际击杀准备目标，Hellfire配普通弩矢与火把以满足可见目标校验。

回归修复保留测试目的：龙底材品牌测试显式准备目标；Orc Cave清场同步移除怪物携带物；珠宝／Mattock使用受控种子寻找真实Ego样本，保留普通神器门槛；骑乘弹药预览使用上面的真实修复。内容断言补入8件源骑乘武器，并将无效发射器形状隔离为无依赖样本。首次桌面导出遇到运行中测试EXE的Windows文件锁，另一次射击准备缺光；修正并完整重跑后通过，失败记录保留，不修改游戏的可见目标校验。

## N2 实现与验收范围

47件均保留源W/P/pval、flags、中文名、激活难度／参数和冷却；源记录固定于`n2-artifacts.txt`。46件走已有底材和普通神器门槛，159『太公望』走已有钓竿的INSTA_ART入口，没有加入普通底材分配。随机附加复用现有构造，额外命名掉落仍单列。共用效果程序按相同输入和步骤复用，不增加协议或保存字段。

补齐装置速度／英雄速度、基础抗性和石肤的单次时长掷骰、装置增强及保留较长状态；保护邪恶使用源100+d25回合。吸取生命对活物不按冥界抗性减伤，并先恢复生命力再回血。方向、目标物品和符号取消先执行真实装置失败判定，成功取消不扣充能、不执行效果。其余球／矢、驱散、魅惑、恢复、探查、化石为泥、灭绝、钓鱼和Escape接既有消费者。

专项覆盖全部47件的真实生成、未知拾取保存、鉴定／装备、成功／失败／取消、装备中完整冷却和中途保存续演；三枚戒指另用实际近战与射击核对额外骰数／射击耗时。有效目标、元素免疫、装置增强、保护邪恶等级条件、墙后驱散排除、魅惑宠物归属及灭绝不发击杀奖励由差异用例验证。普通桌面五件代表为卡拉马斯之、纳雅、弗拉基尔、维尔温德尔和太公望；准备深度、50级XP、无敌、目标与成功种子不代表自然取得或练级。太公望激活前显式暂停浏览器计时器，避免自动钓鱼与逐步保存竞争，激活／保存恢复／手动等待仍走正常命令；自动继续钓鱼由输入控制器测试覆盖。激活日志使用物品实例的投影名称，并保留未鉴定身份；装置判定消息补入正式格式化分支。

**源规则仍有缺口，N2不标为完全闭合：**8／93／122所用GF_OLD_DRAIN尚未实现对怪物最大HP的永久削减；84／128的Escape跨层分支直接执行，缺少原版确认取消交互。25摧毁门沿用现有可通行`door-broken`地格，未变为原版floor。REGEN、普通传送行动耗时及未开放身份钩子仍沿用N1列明的适配。上述差异不由构建成功或47件定义数抵消；额外命名掉落也未因普通生成通过而关闭。

阶段验证通过：核心1843项有效测试（全量初测1840通过，3项修复后定向复验；10项ignored），内容178项（1处旧骑乘清单复验）、Importer216项（7处Q5后未更新的城镇schema断言复验）、本地化39项。N2专项9项及47件源参数契约通过，C估值298例／529个正式实例、103个Build适用性检查、26条未刷新契约、相关Clippy／格式／内容lock检查通过。前端日志及输入59项、类型检查通过。普通Tauri standalone五件代表20步UI操作全部保存恢复并核对哈希，报告`test-results/non-quest-n2/report.json`。

回归修正保持原测试目的：出生种族筛选隔离绕过普通底材表的instant候选；QUESTITEM三叉戟用例排除已经生成的Ulmo；挖掘Ego取消改为真实装置即时成功分支后仅消费一次判定RNG。首次桌面18步后，自动钓鱼与逐步存档检查竞争；显式暂停计时器后整组重跑通过。桌面截图发现的激活未知来源／原始判定消息已修复并重新构建验证，失败日志保留。

## 特例及条件的核对重点

N3其余成员包括链锯剑85／斩首剑92的专属打击分支，诺统74对特定怪物的杀伤，斩铁剑150、王者之剑二世179、天之钉341的目标例外，铁球173的阵营变化，小刺175与已有132的双持组合，金锤195、丘比特之箭270的特殊命中结果，神盾之牙208的投掷返回、妈妈狙击弩381的任意弹药、银锤335的复活抑制／指定怪物眩晕等。源引用集中在`combat.c`、`cmd1.c`、`cmd2.c`、`py_throw.c`、`equip.c`、`xtra1.c`和`xtra2.c`。294同时有当前锋锐分支与未开放Time Lord分支，须分开记证据。272、279、362分别核装备例外、诅咒／高速效果与无敌穿透，不按普通静态装备直接关闭。

N4先确认已有激活是否完整表达源语义：如199的`get_bloody_moon_flags`、144成长与强化、255伸缩／装备形态，以及3、166、204、217、278、293的复合效果。110／164／213等召唤必须使用真实宠物／召唤分配与保存；现有召唤消费者足够就复用，缺少具体召唤类时只补该类。不为这些成员建立第二套物品或随机神器系统。

N5不能只加十个石头JSON：`xtra1.c:4268–4296`赋予领域，`object1.c`还有身份相关`DEC_MANA/EASY_SPELL`。285／287在真实Chaos／Trump施法入口成立前保留条件；已有书籍不等于可以施法。已开放领域先交付，未开放怪物种族／契约术士收益单列，不为石头虚造职业。

N7不是整组冻结：当前可玩身份能完成的普通分支可先交付，并写明未开放加成。146的构造／装备／吸血鬼身份必须追`artifact.c`与`equip.c`，当前已有吸血鬼种族不等价于自动满足所有原版身份转换。233／354不能为现有角色隐式补性别。245／246已有竖琴底材，Q5长笛也已有无弹药弓槽装备消费者；先核普通使用路径，Bard专属收益继续等真实职业。395需专用底材与真实激活，Rage Mage的额外被动独立记录。

## 已定义非QUESTITEM的额外获取

非QUESTITEM通常可普通生成，但也可能有额外命名掉落。先核2／15、65／70／99、194／42；随后332、13／14，以及本计划交付后的84／128、43／55／360等。须由真实可达怪物／地点触发，复用现有命名掉落、唯一性与替代规则，逐项核概率顺序。它们是已有定义的入口补全，不计作新增177件，也不借此扩展Q阶段的13件条件队列。

## 验证与交付

用户于2026-09-13明确调整：**每个大阶段完成后再编译验证，例如N1全部完成**。各小组加入实现、必要测试代码和来源记录，只做静态核对；编译、测试执行、内容lock刷新及相关审计集中到所属大阶段结束。N1小组实现期间未运行这些检查；65件齐备后已完成统一验证，Q1–Q5旧证据不覆盖N1新增内容。

每组保留源中文名／参数、正式普通或instant入口、未知拾取／鉴定、实际装备／战斗／激活、卸装、唯一性与保存续演证据。首件贯通后，同组只补差异，不复制静态矩阵。受控深度／HP／种子准备必须标注，不称为自然练级或通关。

内容变化及时更新包版本与来源映射，lock及受影响源C估值／生成审计在所属大阶段统一刷新、验证；类型变化才生成Schema／协议。大阶段验收优先跑各小组行为与直接受影响的既有调用者；没有共同状态／RNG／投影影响就不机械重复1826项或全量契约。新增交互或阶段代表流程使用普通Tauri standalone；Android与全通关不附带执行。阶段结束汇总尚缺定义、规则、入口或验收的具体ID。

## 177件候选归属

下表只保存派工ID；中文名、底材、源flag及来源引用仍集中在[逐项清单](../design/remaining-item-coverage-review.json)，不建立第二份属性名册。实施时可调整分组，但必须保持互斥、无遗漏。

| 阶段 | Source ID |
| --- | --- |
| N1 | 6、35、43、55、58、71、72、77、81、87、90、91、103、134、137、140、142、154、155、156、158、160、161、165、167、168、176、189、191、193、198、200、206、210、216、220、221、222、223、224、228、229、231、232、234、250、253、254、274、281、292、295、296、323、325、330、331、337、338、339、349、351、352、360、392 |
| N2 | 4、8、9、10、11、12、18、23、25、40、44、48、61、82、83、84、93、98、108、115、116、119、122、127、128、131、133、149、159、170、184、188、202、205、207、209、211、214、218、225、252、277、333、336、361、363、367 |
| N3 | 60、74、85、92、125、139、150、153、171、172、173、175、179、195、208、270、272、275、279、294、334、335、341、362、381 |
| N4 | 3、110、144、164、166、169、199、204、213、217、255、278、293、353、365 |
| N5 | 282、283、284、285、286、287、288、289、290、297 |
| N6 | 7、16、151、273、364、389 |
| N7 | 138、146、215、233、245、246、354、355、395 |
