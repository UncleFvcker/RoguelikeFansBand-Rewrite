# 剩余物品覆盖计划

状态：I0 已完成；I1 重甲首组、I2 装置首组已接入；I3 神器创造卷轴入口及两件决斗者固定神器已随主线合入。I3 剩余神器、I4–I6 的实际缺口继续分批；I7 改为随机制族增量验收。
核对日期：2026-09-10；合并后代码基线：`befb411eb`；工作树：`codex/realms-items`。本次仅调整计划和覆盖记录，复用提交中注明范围的验证证据，不宣称重新执行了游戏测试。
本次来源为 `D:/codex/Frogcomposband/master` 的 `master` Git 对象，实际提交 `a0d92b6378d148c5262cc236b8fa6ed2ca06a54c`。实施每批时重新记录当时 ref 的实际提交并核对变化。

## 目标与完成口径

逐批补齐当前游戏能实际获取、鉴定、使用或装备，并能保存恢复的原版物品。保留已完成的共享生成系统，按缺少的内容、规则、入口和证据拆分工作。
每批区分“定义存在、规则完成、入口可达、实际验收”；只有 JSON 或来源身份不能记作可玩覆盖。无自然分配、占位或特殊用途的源记录也要有明确归类，不能为了数量把它们全部放进掉落池。

本计划覆盖基础物品、固定神器、装置效果、特殊对象及书本依赖。新职业/种族、完整领域施法、任务与设施各自仍由原方向负责；物品侧交付共享定义和效果，实际交叉依赖按[并行协作](parallel-development.md)交接。
未开放身份或系统保留依赖；本轮可玩物品扩充完成不自动把 `runtimeParityComplete` 改为 `true`。

## 当前依据与清单偏差

| 维度 | 本次只读核对 | 对计划的含义 |
| --- | --- | --- |
| 正式物品 | `items/` 有 385 个定义，内容包 1.412.0 | 新增创造神器卷轴及两件固定神器；定义数量不能直接除以源 kind 数量 |
| 原版基础记录 | `k_info.txt` 有 545 个 `N:` 记录，包含 `0:something` | 先区分有效内容、占位和特殊对象 |
| 底材报告 | 334 mapped、10 no-natural-allocation、3 explicit-only、198 not-imported；自然分配为 368 行 | 新增 source kind 364 的 91 层分配行；固定神器不重复计入 canonical 底材 |
| 原报告遗漏 | 六个 ID 已归位：`crystal-ball`、`mind-stone` 为 source kind 609、624；`eternity`、`palantir-of-westernesse`、`razorback`、`stone-of-mind` 为固定神器（均为 `demo.item.*`） | 前两个记 explicit-only 专用底材，后四个记显式神器，不能重复导入 |
| 当前底材身份 | 347 个不同 `rfbBaseKind.sourceIndex`；剩余 198 个记录含占位 0，实际有效未映射记录为 197；其中 130 个带自然分配行 | 已从剩余清单移除 364；其余项按真实消费者推进 |
| 固定神器 | 392 个源记录；16 个有正式映射，376 个无定义，其中 96 个还缺底材 | 新增 174『快刺』、248 决斗者的；不能因卷轴能造随机神器就关闭其余固定神器定义缺口 |
| 装置 | 121 行中 22 行有配置对应、13 行绑定 canonical 底材；其中 12 条侦测/鉴定采用源式生成 | I2 仍是首组完成；其余效果及原别名继续核对 |
| 共享生成 | 当前九构筑的五范围审计见[审计输入](../design/generation-build-applicability.json) | 决斗者已纳入，八个可读卷轴构筑有实际造物指令覆盖；狂战士仍按规则拒用 |

依据：[正式物品](../packs/rfb-demo-original/items/)、[底材报告](../packs/rfb-demo-original/legacy-base-allocation-audit.json)、[来源选择](../packs/rfb-demo-original/legacy-item-selection.json)、[改编记录](../packs/rfb-demo-original/legacy-item-adaptations.json)、[生成矩阵](../design/ego-contract-audit.json)。
六个漏记 ID 来自正式目录与报告的集合比较；I0 先在临时副本运行同步，确认只有报告变化后更新正式报告。物品、名称、主题表及基础分配行均未变化。

I0 最初的 205 个候选记录按下表分流；I1 已完成其中六件普通重甲。同组不表示同一实现难度，当前剩余集合以逐项清单为准：

| 候选组 | 数量 | 主要边界 |
| --- | --- | --- |
| 近战/挖掘、护甲及龙鳞甲（tval 20–38） | 65 | 普通数值/特性可先补；Poison Needle、Death Scythe、Costume 等须追踪专属消费者 |
| 弹药及远程特殊底材（tval 17、19） | 5 | Black Arrow、Gun、Yumi、Flute 无自然分配行，核对是否专用/神器底材 |
| 光源底材（tval 39） | 25 | 排除已存在的 609、624；这一组没有自然分配行，随实际神器及入口接入 |
| 书本（tval 68、93、95、97、100、104–109） | 43 | 40 本领域/职业书及三条其他书本记录，依赖真实内容与消费者 |
| 卷轴/药水（tval 70、75） | 3 | source kind 364、368、465；364 的神器创造需独立入口闭环 |
| 金币源表示（tval 127） | 18 | 对照现有货币生成与拾取语义，不创建 18 种背包币堆凑覆盖 |
| 其他特殊对象与占位 | 46 | 骨骸、尸体/部位、箱子、雕像、魔法雕像、工具、Card、Rune 等；含占位记录 0 |

## 分批执行

### I0：校准覆盖清单

- 对照 `master` 的 `k_info.txt`、`a_info.txt`、`devices.c` 三类效果表，以及正式物品和真实获取调用者。将基础记录、神器身份和装置效果分别统计，名称逐字采用权威中文表/源字符串；无权威名记 unresolved。
- 校准现有选择/改编记录和底材报告，确认六个漏记 ID 的实际归属。复用 [base_allocation.rs](../crates/rfb-legacy-import/src/content/base_allocation.rs) 的同步路径；先看其输出影响，不把全包重导作为清单刷新步骤。
- 剩余项注明稳定 ID 或源 ID、当前缺口、获取方式、所需消费者和所属批次；分类为可用现有机制接入、缺少具体机制、等待身份/领域/地点、非独立可玩内容。复用现有记录；固定神器和装置仅补现有记录无法表达的对表信息，不新建一套通用审计框架。
- 旧 `audit-demo-items` 的 mechanics-ready 来自 importer 的 flags/行为支持判断，不等于自然入口或完整运行时验收；旧 P3 计划只作来源索引，不复活已完成待办。

完成标准：没有未归类的剩余源记录，已有物品没有被重复列为待导入；得到 I1 的明确首组 ID 和缺口。清单变化只做文档/记录检查；实际同步若改动正式内容则按内容变更验证。

I0 结果记录在[逐项核对清单](../design/remaining-item-coverage-review.json)。这是来源/依赖审查快照，复用底材报告；各行的 `reviewGroup` 提供所属批次、当前缺口、获取路径与消费者，固定神器另列 flags、激活和源 `ART_*` 引用。引用索引不代替后续逐件消费者审查，清单本身不开放任何内容。

- 基础项：204 个有效剩余项及 1 个明确排除的占位记录，全部有分类。43 个普通装备候选、10 个龙鳞甲激活、39 个神器专用底材、43 本领域/自定义书等按实际依赖分流；毒针及镰刀内部表示不再按 importer 的 ready 标签误排为直接可用。
- 固定神器：378 个缺失定义分为 269 个已有底材的内容接入、96 个以底材缺失为首要依赖、12 个身份敏感/竖琴分支、1 个零 rarity 专用奖励。98 个缺底材是跨分类统计，不与前述数字相加。源 332 的 W 行尾值按原版解析器忽略，不能擅自“修正”为推测重量/价格。
- 装置：wand 39、rod 35、staff 47，共 121 行。14 行找到现有改编物品对应，其中只有 3 行属于 canonical 底材单项配置；107 行缺对应装置配置。112 行可找到同 token 的既有随机神器 profile，9 行不能直接复用该池；这不代表源装置参数或生成分布已实现。`tsunami-wand` 当前为锥形水伤害，与源 `BALL_WATER` 的球形不同，已列入 I2。
- 中文依据：kind/artifact 中文源表及 `do_effect(SPELL_NAME)` 原字符串已记录，未自行翻译；本清单 unresolved 为 0。模板符号及 HEAL 随效果量选择的两个名称原样保留。
- 记录与工具：修正四本 Sorcery/Nature 书的 `sourceId` 标点归一化，稳定 item ID 不变。现有 `audit-demo-items` 显式传 `-` 可只盘点当前物品，读取正式 `rfbBaseKind`，并拒绝未知、冲突或重复 canonical 身份；提供旧 plan 路径时仍严格检查旧 P3，不补填历史完成标签。

当前盘点复现：设置 `RFB_LEGACY_SOURCE` 后运行 `cargo run -p rfb-legacy-import -- audit-demo-items packs/rfb-demo-original/legacy-item-selection.json packs/rfb-demo-original/legacy-item-adaptations.json - packs/rfb-demo-original/items`。
I1 首组完成后，当前命令输出 544 个有效源记录、346 个 active、49 个 importer mechanics-ready 和 149 个 blocked；它不检查正常创角可达性或执行游戏行为测试。基础分配的刷新命令仍见[内容开发](content-development.md)。

**I1 首组六件普通重甲已完成：**272 金属鳞甲、274 双层环甲、276 双层链甲、277 条板链甲、278 金属布面甲、279 板环甲。中文显示沿用源模板的现有单数化规则。
通过现有 `sync-demo-items` 只导入六个 ID，再用基础分配同步接入正式池；源等级为 25/25/30/35/35/35，每件保留一条 `A:等级/1`。护甲、命中、重量、估值、酸销毁及 Ego 复用已有规则，未新增专属激活或神器。

### I1：补普通装备底材

- 优先处理现有数值、槽位、抗性和攻击表达已足够的底材：从 65 个装备候选中筛选普通护甲、头盔、盾牌、挖掘工具和普通武器。按机制族小批提交，具体数量由 I0 核实，不承诺一次补齐 65 件。
- 复用装备定义、估值、销毁、Ego、随机神器及基础分配路径。保留源等级、分配行、零概率和主题谓词，不能均匀撒入所有商店或掉落表。
- 龙鳞甲另作小批：复用已有底材生成，逐项核对抗性与激活，确认 Ego/随机神器生成后仍保留应有底材能力。毒针、死神镰刀等专属战斗分支留给 I4。

完成标准：实际生产分配路径生成目标机制族，鉴定/装备后属性、攻击或防御消费者正确；新实例保存恢复一致。候选池扩大引起的 RNG/契约变化有可解释差异，完成受影响契约检查。

首组证据：[核心物品测试](../crates/rfb-core/src/game/tests/items.rs)中的 `ordinary_heavy_armor_allocation_reaches_equipment_and_save` 从人类战士新角色开始，受控设置生成深度 35，反复调用正式普通掉落生成，保留完整候选池、权重与质量抽样。六种普通品质实例均实际拾取、鉴定、装备，验证现有 AC 换算、命中惩罚与负重；保存恢复后，下一次生成的实例、ID 和 RNG 一致。这是核心受控前置验收，不是自然练级或桌面试玩。

I1 该批的 26 条 active 契约全部通过，无需刷新 fixture 或调整 State Hash Schema；当时候选池虽扩大，现有 fixture 的观察结果未变。生成矩阵覆盖 152 个装备底材，八构筑适用性检查通过。逐项清单移除本组六个 gap，并更新两个固定神器的底材依赖；其他 37 个普通装备候选和 10 个龙鳞甲激活候选仍待后续小批。

### I2：装置效果与普通消耗品

- 先核对三类源装置效果表、当前别名物品、已有 effect program 和实际生成入口。已有激活或法术效果复用同一执行路径，但重新核对装置自身的难度、费用、强度、等级/模式筛选和充能恢复。
- 按实际缺失效果族接入自然装置生成；不为每个效果复制一种近似底材，也不把随机神器的 195 项激活池当成三类装置的完整原版效果表。
- source kind 368、465 分别追踪真实使用分支和现有能力；若缺机制，在本批补最小完整实现或明确转交对应依赖。364 已由 I3/竞技场方向交付，不重复实现。

完成标准：真实获取、未知/已知状态、选择目标、成功/失败/取消、费用和恢复，以及保存后继续使用都符合规则。狂战士等实际使用限制沿用职业审计；不能用“职业不能用”删除普通生成候选。

首组使用 RFB master `a0d92b6378d148c5262cc236b8fa6ed2ca06a54c` 的 rod 四条、staff 八条侦测/鉴定配置，接入现有 canonical 底材；不增加基础 kind。效果名称逐字取 `devices.c::do_effect`。自然生成采用源等级提升、正态 power/费用/容量、rarity 随深度衰减、Good/Great 筛选与一次 `level+2` 普通重试；固定给予仍使用指定效果及满能量。独立实例在察觉效果或鉴定后显示激活/能量，成功取消鉴定保留检定 RNG，但不耗能量或时间；恢复保留源百分之一 SP 的取整语义。商店仍使用既有固定库存入口，`STOCK_TOWN` 仅记录来源，本批不新增随机商店模式。

验证覆盖正式完整池的受控深度生成、拾取、逐实例知识、侦测空结果与成功/失败、完整鉴定与取消、狂战士拒用、恢复、保存与后续生成/RNG。12 条是本组已接入范围，其余 109 条源表行仍须后续核对（包括已有 wand 单项及改编别名）；不代表三类装置全量分布已还原。

普通消耗品依赖已追到具体分支：疯狂卷轴（368，`devices.c` 的 `SV_SCROLL_MADNESS`）含装备诅咒、连锁 Ty curse、毁灭、召唤赛博恶魔、法力爆炸、制造及神器创造。364 已交付，解除“缺神器创造消费者”的依赖；疯狂卷轴仍缺自身随机分支及目标/取消/消费绑定，复用现有消费者前须核对源调用语义，不直接标为完成。液态洛格鲁斯（465）继续作为 I2 独立机制族：饮用按幸运修正 d13，涵盖永久降属性、变异、Ent 变形、恢复/治疗和重伤等；破碎在 `spells3.c::potion_smash_effect` 中为 1/5 治疗、否则混沌。`chaoswar.c::nonlethal_ty_substitute` 实际执行禁属性洗牌的变异、随机永久降属性及装备诅咒；现有 `chaos_patron.rs::apply_nonlethal_ty_curse` 仅伤害/混乱/眩晕，不能代替。两种消耗品尚未开放。

### I3：固定神器与神器创造入口

**已交付，移出开发待办：**创造神器卷轴（kind 364，`4079b1131`）已有 91 层普通分配与竞技场末守卫奖励。Rust 投影合法目标，界面支持堆叠损失确认和可选命名；复用 `SCROLL|GOOD` 工厂原位改造、名称登记、职业 bias、德行及取消/失败退款。八个可读卷轴构筑、五项卷轴边界测试和竞技场“领奖→拾取→造物→保存”核心链已有证据；合并提交 `befb411eb` 复验了相关交叉影响。这里关闭的是规则和入口，专属桌面流程转入 I7。

固定神器 174『快刺』和 248 决斗者的也已合入，含旧城堡奖励、重复神器替代、扫射激活及职业攻次限制；见[决斗者来源审计](duelist-source-audit.md)和[奖励测试](../crates/rfb-core/src/game/tests/duelist/rewards.rs)。固定神器覆盖为 16/392，剩余 376 个定义继续分批。

- 从逐项清单按“实际地点/职业依赖 → 现有底材及消费者可用 → 等待专属系统”排序，每个机制族单独交付。现有 16 件不重复导入。
- 补完整基础属性、flags/pval、诅咒、激活、普通/instant 生成、唯一性登记和获取限制。复用 [loot.rs](../crates/rfb-core/src/game/loot.rs)、现有神器物化及激活消费者，不另建神器工厂。
- 身份敏感神器和固定神器竖琴先查原版普通分支；当前职业有适用分支就同时实现，未开放身份保留依赖。同步审计输入及 `audit-egos.mjs` 中现有固定神器断言，不能只删除失败断言使新增内容过关。
- 地点联动队列对接[奥林匹斯计划](../design/mount-olympus-dungeon-plan-20260910.md)：按该批实际启用的神祇清单逐项补 special-drop，含宙斯神器 256；不能只补最终守卫，也不预先导入所有神系神器。Ambrosia 是地点食物/获取限制工作，与固定神器分开记录；实际启用前核对两方向已提交内容。
- Inspired Smithing 重铸仍依赖真实设施/入口；Android 装备经验仍依赖未开放身份系统。二者单列，既不算本次卷轴完成，也不阻塞现有可玩身份的固定神器小批。

完成标准：至少一个符合源规则的实际入口；唯一性、身份差异、激活/诅咒等真实消费者及恢复后的登记/RNG均有证据。测试明确标注受控前置，不把强制物化当自然获取证明。

### I4：特殊底材与交互对象

- 优先处理当前可玩身份已有明确交叉点的战斗族：毒针、死神镰刀分别小批推进，核对决斗者的武器/攻次限制及冬贝利反噬分支。合并后的决斗者代码可复用，但这两件底材及其源消费者仍未交付。
- 按源调用链分成小批：毒针/死神镰刀等战斗规则；箱子生成、陷阱和开箱；魔法雕像及目标怪物身份；骨骸、尸体、部位、雕像/照片；哨、号角、工具、铁钉等交互物。无当前消费者的专属对象保留具体依赖。
- 卷轴批已补弹药实例骰数的射击/估值消费者，以及 Snotling 可复用神器蘑菇及冷却；相关能力从“需新建表示”改为优先复用。它们不代表箱子陷阱、雕像怪物身份或特殊武器规则已经完成。
- 每组先确认当前模型是否足够；若确实需要新实例字段，先完成该字段的初始化、保存、哈希和消费者再开放物品，不给所有物品预造额外状态。
- 宝箱、任务或设施内容由实际入口方向使用同一物品 ID。金币先核对当前货币生命周期与原版生成，不强制物品化；占位和只用于特殊构造的底材不进入普通池。

完成标准：物品本身能完成对应交互，含必要失败/拒绝边界及生命周期；有来源限制的获取遵守来源限制。不能用无效果占位物关闭覆盖缺口。

### I5：书本与领域/职业依赖

- 逐一核对 43 个书本候选的实际消费者。Craft 的四册/32 法术属于完整领域接入工作，已有 Craft Ego 制作不代表该领域已完成。
- 分开两类工作：已有八领域的 32 本书不重导，其中只有 Death 开放普通创角，另外七领域的菜单/流程验收随领域方向推进；43 个缺失书本候选则需要其完整领域/职业消费者。Chaos、Trump、Craft 等按实际接入范围分批，不能把“已有内容未开菜单”混成“缺全部法术”。
- 本次决斗者没有新增施法领域或书本，43 项缺口没有因此减少；新构筑审计基线更新为九构筑，不自动新增其他领域入口。
- 物品侧同时维护源书身份、获取、发现计数、等级/质量筛选、学习/销毁消费者；新领域 Build 按[生成接入闭环](content-development.md#职业与领域-build-的生成接入)补审计。未经领域/职业规则验证，不单靠摆出书本宣称可用。

完成标准：有完整领域/职业依赖的书本具备获取、发现、学习/使用及保存证据；尚未开放的依赖明确列出。已有领域的菜单开放和未导入领域的完整接入分别计数。

### I6：对象表示差异

- 神器卷轴已实现自身的整堆目标校验、多余弹药销毁、成品单实例、原实例 ID/位置保留和 `ArtifactCreation` 来源；本地能力不再重做，也不据此宣称全局来源或堆叠模型完成。
- 复核 [B1/B5](../design/base-allocation-acquirement-plan.md) 保留的书本单实例、普通物品生成堆数，以及 `obj_can_combine` 的来源、铭文、折扣等差异。先列真实消费者和可观察差异，再决定最小状态变化。
- I1–I5 某组物品确实依赖这些语义时，将必要部分提前随该组实现；其他差异独立推进，不能作为所有内容接入的前置重构。
- 改动数量/合并语义时覆盖生成、落地、拾取、拆分/合并、交易、储存、销毁和发现计数中实际受影响路径，以及保存后的后续行动。不能靠存档默认值或刷新哈希掩盖丢失。

完成标准：逐项关闭有行为证据的表示差异；仍采用改编表示的项目写清边界，全原版完成标记据实保留。

### I7：本轮可玩范围验收

- 改为随机制族交付，不等待 I3–I6 全部结束。汇总新增身份、效果、入口及未关闭依赖，核对正式报告和九构筑审计一致；没有新增影响的旧验证沿用已注明范围的证据。
- 下一次 I7 小批优先验证当前已交付但缺专项桌面证据的两条链：I2 动态装置获取→逐实例鉴定→侦测/鉴定及取消→能量恢复→原生读档继续；创造神器卷轴获取→合法目标/堆叠确认/命名或取消→成品装备/射击→原生读档继续。补 Snotling 蘑菇复用冷却这一特殊消费者，采用明确标注的稀有/等级前置。
- 创造神器卷轴及竞技场 AR3–AR5 当前是核心/界面自动验证，合并提交也明确未新增 standalone。决斗者专项证明其自己的新开局、挑战/战技与保存继续；B6/E8.8 证明当时的共享生成/装备范围，均不能替代新卷轴或新装置的桌面操作证据。
- 对本轮新增的交互和机制族安排 Windows Tauri standalone：新角色 → 真实获取 → 鉴定 → 装备/使用 → 原生保存恢复 → 继续行动。稀有或高等级前置可受控准备，但必须明示，不能冒充自然练级。
- 单纯增加同机制数值内容不要求每件各跑桌面测试；新界面、目标选择、特殊实例等真实风险补对应检查。Android 与全仓构建不自动附带。

完成标准：可玩范围与证据对应，未完成项仍可追踪；不以文件数、构建成功或审计绿灯代替行为结论。

## 执行顺序与验证

合并后不再把 I3–I7 当作串行大阶段：先补当前 I2/神器卷轴的 **I7 增量桌面验收**；开发继续按 **I3 实际地点/职业所需神器小批、I4 当前身份相关特殊武器小批**推进。I2 疯狂卷轴不再等待 364，但须独立完成自身分支。I5 随真实领域/职业接入；I6 只在所选小批需要时提前处理。奥林匹斯神祇清单、箱子/雕像及其他交互族按实际依赖另组，不扩大为一次性全量导入。

每次先复查本工作树及主线已新增的物品 ID，防止与职业奖励或地点神器重复。只在同一状态/文件确实交叉写入时协调，不新增共享文件审批。
内容变化更新正式包版本及 lock，类型变化才生成对应 Schema/协议；按[内容开发](content-development.md)使用现有命令。普通候选池扩充仍可能改变全局 RNG 消费，按实际影响执行[验证与契约](testing.md)，说明差异再刷新必要 fixture。
新增物品触及职业/主题/神器条件时同步来源审计、生成矩阵和只读 CI 检查；通用物品不机械复制九构筑全套测试，仅补实际差异与缺失证据。

I0 的盘点回归覆盖当时身份计数、别名去重、显式跳过旧 P3 及错误身份拒绝。I1 首组、I2 首组和本次合并各自已有注明范围的验证记录；后续继续按机制族推进，I7 每次只补新交付的桌面影响。
