# 固定神器后续导入计划

审查日期：2026-09-11。A0 基线为三方向合并后的 `2ca665941`，物品分支已快进同步；A0 已完成。A1 两件光源及 A2 六件护具已补实现和测试，均尚未编译或运行；A2 基于 `2d50a9d3a`，A3–A10 尚未实施。

**本轮用户约定：A1–A10 过程中不编译，A10 结束后统一编译验证。**各批可补实现、测试和来源记录，只做静态检查；“实现已补齐”不表示验收通过。包版本和 lock 暂留最后已验证的 1.422.0，当前新增源内容尚未纳入该锁；需要 Rust/C 编译的内容摘要、来源派生报告和估值参考也集中到 A10 更新，不手工猜测 hash 或修改生成报告冒充通过。

RFB 来源为 `D:/codex/Frogcomposband/master` 的 `master` Git 对象，本次实际提交 `a0d92b6378d148c5262cc236b8fa6ed2ca06a54c`。后续每批重新确认 ref 并记录实际来源；中文名逐字使用对应中文表或源字符串。下文数字均为 **artifact source ID**，底材 ID 另行注明。

## 旧计划完成边界

| 旧项目 | 已完成范围 | 仍未完成 |
| --- | --- | --- |
| 奥林匹斯同步 | 256–267 十二件、OL1–OL4 核心入口及 OL5 文档交接已随 `2ca665941` 同步 | OL5 不代表新增桌面验收，原计划的地图／身份适配边界继续保留 |
| 普通护具 | 47、52，两件目标均完成 | 普通护具类别未清空 |
| 普通武器 | 79、86，两件目标均完成 | 普通武器类别未清空 |
| 高等抗性装备 | 21、24、27、28、36，五件目标均完成 | 其余 `XTRA_H_RES` 和其他随机附加组合仍待接入 |
| 光源与激活 | 1 加拉德瑞尔的玻璃瓶及底材 605 | 只完成首件，其他光源、激活和特殊消费者仍开放 |
| 身份／特殊战斗／零稀有度 | 41『恐惧面具』的当前身份分支及激活 | 只完成首件；未开放身份分支、其他身份神器、特殊攻击和 192 奖励仍开放 |

本分支现有正式映射 **53/392**，剩余 **339**；其中 A1／A2 新增八件尚待统一验证。原物品分支 42 件和法师方向 **33、120、249** 已去重归位。定义数量不代表所有原版消费者或桌面流程均已验收。规则／入口证据见[总计划 I3](remaining-item-coverage-plan.md#i3固定神器与神器创造入口)及对应提交。

## 下一轮执行顺序

每行是一个明确范围，完成全部列出的 ID 才能关闭该行。允许按独立消费者拆提交；发现阻塞时留下具体 ID、源分支和缺失消费者，整行保持开放。候选已有底材或同名效果，不等于已通过逐件行为审查。

| 顺序 | 明确目标 | 实现与验收重点 |
| --- | --- | --- |
| A0 同步去重（完成） | 主线 33、120、249；法师及共享随机能力实现 | 已同步 `2ca665941`，清单 45 件映射与正式包一致。现有审计输入／生成矩阵保留 56 个法师组合及高阶法师工艺等全部 66 构筑，只读检查通过。法师恐惧面具专项补齐读档后实际施法拒绝、不消耗状态／法力／RNG、除咒后固有影响及卸下后法力上限／施法恢复。复用已有 `XTRA_POWER`；OL5 交接记录已同步，无新增运行时内容 |
| A1 光源后续（实现已补齐，待 A10 验证） | **2、17**：埃兰迪尔的星星、学识之石头；底材 **606、608** | 已接专用底材与现有 instant 生成、免燃料半径 3；2 映射半径 30 后照明 3d15／半径 3，冷却 250 tick；17 完整怪物探知，冷却 50 tick，补 `LORE2` 穿戴时普通鉴定背包，复用后续脚下物品鉴定。测试已写未运行。2 与已有 15 的萨鲁曼专属掉落配对另列后续入口，本批获取范围为普通 instant |
| A2 普通护具（实现已补齐，待 A10 验证） | **37、197、276、346、358、359** | 六件定义沿用现有底材，无激活或随机附加。复用法力容量 +15%、反射、属性维持、潜行／自由行动、负魅力、装备照明 -1 及亡灵 ESP 消费者，无新增运行时规则或类型。已补生成／装备／保存用例，扩展现有反射边界测试，并补『睡帽』照明／感知专项；均未运行 |
| A3 普通武器 | **95、96、100、106、114、118、178、347** | 『奥松迪尔』『提尔-伊-阿克』、伊欧林加斯的、『巴鲁克海雷德』『血刺』『纳尔-伊-瓦吉尔』、参孙的、流浪汉的。八件均有底材，无激活或生成附加抽选。验证伤害、品牌与杀伤取高、骑乘、祝福及实际装备被动，不用静态 flags 对表代替攻击证据 |
| A4 高等抗性续组 | **30、31、32、62** | 索林之、凯勒巩的、阿纳瑞安的、索尔之。复用第四组 `XTRA_H_RES`；保留源 12 项顺序、一次抽取、允许重复且不重抽。覆盖已有固有抗性、酸免疫及未知状态保存 |
| A5 抗性或能力 | **64、73、75、76、135** | 迈兹洛斯的、『敌击剑』『兽咬剑』『古桑格』、阿扎格哈尔的。复用既有 `XTRA_RES_OR_POWER` 路径，先核实权重、物化与知识。当前 affix 用单次加权抽取表达分布，并非原版分支抽取的逐次 RNG；明确沿用的改编，不宣称源 RNG 完全一致 |
| A6 双重随机附加 | **117** 奥力之 | 单独关闭“先随机能力、再高等抗性”的源顺序。主线通用 `XTRA_POWER` 在 affix 之后执行，直接与高抗 affix 拼接会颠倒顺序；在现有物化路径作最小修正，并验证不重复抽取、不影响恐惧面具与法师神器的已成立语义 |
| A7 光源激活续组 | **147、291**；底材 **610、621** | 拉普达的、战争之石。核对法力球及狂暴激活的源参数、目标与冷却，战争之石的装备近战加成不能遗漏。底材 621 也被 297 使用，本批不因此顺带开放末日领域消费者 |
| A8 装备激活续组 | **53、54、56、57**，再 **66、67、68** | 前四件覆盖魔法飞弹／元素激活及手套品牌的实际攻击归属；后三件同时带投掷和高抗抽选，须验证投掷路径。逐件核对源 power、额外参数、冷却、取消／失败消费与保存；相同效果 token 不能替代参数审查 |
| A9 当前身份普通分支 | **145、322** | 小龙的、『龙枪』。先完成当前职业可用的普通武器规则及生成，不等待武僧或龙契约术士开放；这些特殊身份的专属分支逐项保留依赖，不能把普通分支完成写成全身份完成 |
| A10 零稀有度奖励 | **192**；底材 **245** | 保留 rarity 0 和 `QUESTITEM`，排除普通池。追通 Greater Hell-Beast 的实际可遇入口、死亡专属奖励、重复神器替代、拾取及读档后唯一性。现有怪物定义本身不证明入口已开放；入口缺失就明确保留阻塞，不以强制掉落完成验收 |

A1–A10 共 **38 个不同神器 ID**，不含 A0 已同步的三件。A1／A2 的八件已补定义，下一批为 A3；A6/A8/A10 的机制问题不阻塞其他独立小批。此表是后续开发范围，不承诺所有候选均可只改内容导入。

## 完整余量与后续队列

以[逐项清单](../design/remaining-item-coverage-review.json)的 `fixedArtifacts` 为完整名册：直接筛选 `formalItemId == null` 即得到全部 **339** 个待导入 ID；非空仅表示已有定义，验证状态另见 evidence。不要另造第二份全量名册。

| 当前互斥审查组 | 数量 | 含义 |
| --- | ---: | --- |
| `artifact-content` | 248 | 有底材的内容候选；仍须检查源专属消费者与入口，不能整体标为可直接导入 |
| `artifact-missing-base` | 80 | 以缺底材为首要依赖 |
| `artifact-identity` | 10 | 身份分支优先审查，其中部分也缺底材 |
| `artifact-zero-rarity` | 1 | 192 专属奖励 |

跨组还有 **82 个缺底材、70 个带 `QUESTITEM`、154 个带激活**，这些是重叠依赖，不相加。排除 A3–A10 的 30 个明确目标后仍有 **309 个**，按以下队列持续展开，每轮都从完整名册重新筛选，不能把本轮表格完成当作 I3 清空。

1. **领域石十件：282–290、297。**与当前已开放法师／高阶法师及工艺领域一起审查。源 `xtra1.c` 的 `easy_realm1`、`object1.c` 的身份相关 `DEC_MANA/EASY_SPELL` 和激活共同构成行为，不能只导入石头与激活。每批按实际领域消费者分组，连同专用底材交付；缺失职业的差异单列。
2. **普通装备和通用激活余量。**继续从 `artifact-content` 逐件确认，而不是到 A2–A8 就停。已有明确后续护具候选 132、238、357、380；49、50 等缺专用底材的装备随本件补底材。特殊情况先剔出：60 的生成另有概率门槛，74 对特定怪物有专属杀伤，不能按普通速度鞋或普通屠龙剑处理。
3. **剩余身份敏感内容。**原十件为 78、144、145、146、190、212、245、246、320、322；A9 处理两件后仍余八件。竖琴 245/246、石鬼面 146、专属武器及 `QUESTITEM` 分别按实际身份和入口推进；320 还缺底材。41 的 Mauler 与 Monster Hydra/Troll/Jelly 分支也继续列为未开放身份依赖，虽然其定义已计入覆盖。
4. **特殊战斗、身体装备和地点神器。**以逐项清单的源 `ART_*` 引用为起点追实际调用链。例如 125/153 的弓箭组合、136/379 的雷神装备、171/172 的双刀、268/269 的身体装备，不能拆成静态 flags 后宣称完成。每件区分普通生成与专属掉落；`QUESTITEM` 只能随真实地点／守卫／奖励入口交付。引用清单只是索引，无引用也仍须检查通用 flag 消费者。

以上队列会交叉覆盖同一件神器，排期与计数始终按 source ID 去重。长期身份／地点依赖保留在现有逐项记录，不为它们给普通候选增加前置系统。

## A10 统一验证清单

以下工作明确留待 A10 结束后执行，不能引用 A0 的通过结果替代：

- 重生成基础分配报告，核对新增专用底材无自然分配；运行 `generate-item-value-reference.py --sync` 生成本轮新增物品的源 C 估值，复核旧项差异。随后更新包版本，通过 `inspect-source` 取得真实摘要并同步 lock，再执行 `verify-source`。命令见[内容开发](content-development.md#内容锁与生成文件)。
- 运行本轮新增测试及直接受影响的既有测试。A1 为 `game::tests::items::a1_instant_lights_generate_activate_and_resume_after_save`，另包括 Galadriel 完整普通池 instant 测试、共享生成／知识／装备／装置、Sniper 探知及源估值相关测试；后续批次按实际改动补充此范围。新测试覆盖受控底材／深度下的真实稀有度抽取、未知拾取保存、装备与自动鉴定、激活、冷却末 tick 和读档继续，不代表自然练级或桌面验收。
- A2 新增 `game::tests::items::a2_armor_generation_equipment_consumers_and_uniqueness_survive_save`、`game::tests::lighting::nightcap_reduces_carried_light_preserves_glow_and_senses_only_undead_after_save`；现有 `p86e_mirror_shield_reflects_monster_bolts_once_with_exact_three_of_four_gate` 和 `mirror_shield_does_not_reflect_beams_balls_or_breaths` 已扩展至珀耳修斯盾。A10 运行这些及直接受影响的普通神器生成、护具、法力容量、照明／感知用例。六件源估值、内容与本地化也纳入统一检查；没有类型变化，不重生成 Schema／协议。
- 运行本地化与来源／内容检查，重生成并检查共享生成审计。正式池新增候选会影响既有 RNG，执行直接受影响的契约；后续若改变共享状态输入或公共 RNG，再按实际范围扩大。不得为通过而无解释刷新 fixture。
- 修复统一验证中发现的问题，再将各批状态由“实现已补齐”改为实际通过范围。A1 沿用现有 REGEN 的每 10 world tick 回复 1 HP 改编；怪物探知不投影源 XP 栏。萨鲁曼在源 `xtra2.c` 先等概率选 2 或已有 15（`ART_PALANTIR`），再分别应用 66%／15% 掉落机会；此额外获取入口仍开放，不能随普通 instant 通过一并关闭。

## 每批关闭条件（本轮运行性检查统一延至 A10）

- **内容定义：**完整源参数、权威中文名、底材和来源记录；正式包版本与 lock 更新。仅类型变化才生成 Schema／协议，不手改生成物。
- **规则与入口：**逐件检查 flags/pval、诅咒、随机附加、激活和专属消费者；至少一个符合源规则的真实获取路径。普通池／instant／专属奖励分别取证；受控稀有度和等级前置须明示。
- **保存与唯一性：**从新角色出发，验证生成、拾取、未知状态／鉴定、装备或使用、保存后继续；覆盖本批实际随机或冷却边界。不以直接物化代替普通生成，也不为每件复制一套相同测试。
- **检查范围：**运行直接受影响的既有测试、内容与名称／来源校验、lock 和生成审计；公共 RNG 或状态输入变化再按实际范围执行全局契约。纯内容变化不自动刷新 fixture，不因普通合并重跑全 workspace。
- **交付口径：**分别报告定义、规则、入口和验收；专属桌面证据留在 I7，明确未执行的部分。需要交付可玩桌面产物时使用 Tauri standalone；既有构建或核心受控测试不替代桌面流程。

每批结束只更新当前映射、待办和长期依赖，具体完成细节放提交说明，不继续向本计划追加批次流水账。
