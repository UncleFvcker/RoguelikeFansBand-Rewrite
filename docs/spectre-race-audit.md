# 幽灵种族专项审计

审计日期：2026-09-09。重写基准：`084f341f0`。原版唯一规则依据为 `D:/codex/Frogcomposband/master` 的 `master` Git 对象 `a0d92b6378d148c5262cc236b8fa6ed2ca06a54c`；以下原版路径、行号均指该对象，不指工作区文件。

**五步已按计划范围完成：来源审计、种族被动、合法穿墙、墙内伤害、出生补给、恐吓能力与现有关联消费者已接入或验证，正式创建已开放并通过专项验收。** 过程见[实施计划](spectre-race-plan.md)，最终验收见第八节。生命力耗尽的永久转化仍缺失，见第六节；不将该缺口计为已实现。

第二步复用现有 Race、状态、伤害和移动管线；新增 [wall_passage.rs](../crates/rfb-core/src/game/tests/wall_passage.rs) 的 12 个专项测试全部通过，涵盖当前/本体种族、150 能量、密度/挤压、0 HP、状态到期、SP 抵扣与恢复、骑乘/下马/坠马、保存及离层缓存。相关既有核心回归 188 个、导入/地形校验各 1 个、前端事件格式 32 个测试通过；前端类型、Rust 格式、相关 crate 的 Clippy 和内容锁验证通过。`movement / status-effects / equipment / combat / progression / inventory` 六类共 12 个契约通过，未刷新基线。未运行完整 E2E 或生成本阶段桌面试玩包。

## 1. 种族定义和中文原文

原版 `src/races_k.c:846–916`：`_spectre_get_powers`、`_spectre_calc_bonuses`、`_spectre_get_flags`、`_spectre_birth`、`spectre_get_race`。

| 项目 | 原版值 | 重写现状与实施位置 |
| --- | --- | --- |
| 身份 | 幽灵；索引 27 | [Race](../packs/rfb-demo-original/races/spectre.json)为 `rfb-legacy.race.spectre`；非生物、不死、缓慢消化、夜间和吸能已接入，保留导入/变形候选，正式入口已开放 |
| 中文说明 | `spectre_get_race` 的完整 `me.desc` | [中文内容](../locales/zh-CN/content.ftl)保持原文逐字一致；第五步已替换英文占位并补八条规则说明 |
| 属性 | 力 −5、智 +4、感 +2、敏 +2、体 −2、魅 −3 | 已有，无需重写 |
| 基础数值 | 生命 90、基础 HP 13、经验 250、红外 5、商价 135 | 已有；第二步补齐红外 5 |
| 技能 | 拆除 10、装置 10、豁免 12、潜行 5、搜索 5、感知 14、近战 −15、远程 −5 | [技能集](../packs/rfb-demo-original/skillSets/race-spectre.json)已有对应值 |
| 防护 | 寒冰/毒素/虚空抗性；保持生命 +1；识破隐形 +1 | 已接入当前形态统计管线；毒抗不等于所有毒效果免疫 |
| 其他被动 | 浮空、缓慢消化、穿墙 | 已接入现有字段/标签及通行管线；覆盖形态切换和实际营养消耗 |
| 标志 | 非生物、不死、夜间出生、装置吸能 | 已接入现有标签，导入报告同步识别已映射 |
| 出生 | 一根 `TV_STAFF / EFFECT_NOTHING` 法杖；`py_birth_light()` | 已补满能量起始法杖及通用口粮排除，保留火把与六职业完整装备 |
| 主动能力 | 恐吓怪物；4 级、智力、消耗 6、基础失败率 50 | 已挂接[已有能力](../packs/rfb-demo-original/abilities/race-scare-monster.json)，导入输出及运行时投影一致；不改共用恐惧强度 |
| 同族召唤 | `G` | `kin-glyph-71` 已有，原生/临时幽灵实际使用同族卷轴的召唤结果通过专项验证 |

“恐吓怪物”名称和说明来自 `src/spells_s.c:24`，效果调用 `fear_monster(dir, p_ptr->lev)`。施法失败使用智力；现有共用恐惧强度另有魅力消费者，不应为了种族施法属性改写它。中文内容名继续以原版为准；找不到原版名称时记为未解决，不自行翻译。

## 2. 本体、当前形态与历史出生种族

原版 `src/races.c:8` 的 `prace_is_` 查询当前形态；`get_race`（约 529）优先临时变形，`get_true_race`（约 523）读取 `prace/psubrace`。**`prace` 是可永久变化的本体种族，并非永远不变的 `start_race`。** 首版计划中的“出生种族保底”表述据此修正。

重写 [progression.rs](../crates/rfb-core/src/game/progression.rs)的 `character_definitions` 用 `build.race` 作本体，临时状态 `granted_race_id` 覆盖当前 Race。被动、能力、同族、饮食和不死判断应沿用这个入口；本体特例单独明确，不能用当前 Race 代替。

| 场景 | 原版应有结果 |
| --- | --- |
| 人类暂时变为幽灵 | 获得当前形态的被动、穿墙、吸能和恐吓；墙内伤害没有本体幽灵的限额 |
| 本体幽灵暂时变为人类 | 不保留幽灵种族被动；若仍由其他来源获得普通穿墙，密度分支仍检查本体幽灵并限额 |
| 本体幽灵失去全部穿墙后留在墙内 | 走挤压分支，没有幽灵密度限额 |
| 本体永久变更 | 本体特例随新 `prace` 变化；不能绑定历史创建选择 |
| 死亡领域幽灵形态状态 | `IS_WRAITH` 状态有自己的墙内豁免；不等于变更为幽灵 Race |
| 临时变为幽灵 | 不重跑夜间出生、起始法杖/照明和出生美德 |

## 3. 地形、骑乘与行动能量

来源：`src/cmd1.c:4337` 的 `player_can_enter`、约 5177 的树木代价、5345 的穿墙能量；`src/monster1.c:712` 的 `monster_can_cross_terrain`；`src/xtra1.c:4903` 的骑乘被动调整；`lib/edit/f_info.txt` 的地形旗标。

- 普通通行、可飞越、可游泳和允许穿墙是不同资格。普通花岗岩有 `CAN_PASS`；永久墙没有。边界、占位、结界和特殊地形检查不能被穿墙跳过。
- 永久属性本身不是“一律不可进入”：原版山地虽永久，却可飞越。幽灵的浮空可以使它通过可飞越地形，这不是穿过永久墙。
- 骑乘时由坐骑通行规则接管；穿墙坐骑载人穿墙仍要求骑手也能穿墙。不能仅凭坐骑能力带普通人穿墙，也不能仅凭幽灵骑手带不会穿墙的坐骑穿墙。原版还会在骑乘非穿墙坐骑时清掉玩家 `pass_wall`。
- 成功进入 `FF_CAN_PASS` 地形且没有破墙等例外时，行动能量乘 `3/2`，正常 100 → 150。该计算不需要 RNG，亦不是每走一步立即扣一次墙内 HP。
- 原版树木有 `MOVE | CAN_FLY | TREE`，没有 `CAN_PASS`；幽灵穿行树木免树木额外代价，但不因此收取 150 能量。重写树木为不可步行且 `allowsWallPassage: true` 的适配表达，不能把该字段直接当作所有原版 `CAN_PASS` 语义，造成树木误伤或误加价。
- 传送另有 `FF_TELEPORTABLE` 前提（`src/spells3.c:273`），花岗岩不能因可穿墙就自动成为传送目的地。现有传送候选用可步行格，不应盲目替换成通行判断并放开所有墙。

第二步通行实现与保留边界：

| 消费者 | 当前行为 | 边界 |
| --- | --- | --- |
| [player_stats.rs](../crates/rfb-core/src/game/player_stats.rs)，`player_can_pass_walls` | 当前幽灵或状态授予自身资格，骑乘后再检查坐骑 | 自身资格和有效资格分开，供下马与伤害消费者使用 |
| [mod.rs](../crates/rfb-core/src/game/mod.rs)，普通移动及主动下马 | 共同地形判断；下马用脱离坐骑后的资格 | 保留边界、占位和原有交互 |
| [movement.rs](../crates/rfb-core/src/game/movement.rs)，坐骑/怪物通行 | 穿墙须地形允许，载人坐骑还须骑手资格 | 普通怪物不附加骑手条件 |
| [travel.rs](../crates/rfb-core/src/game/travel.rs)，寻路 | 使用相同地形规则及合法破墙目标 | 保留已探索、已知地形和陷阱过滤 |
| [wilderness.rs](../crates/rfb-core/src/game/wilderness.rs)，局部荒野/世界地形 | 表层判断复用共同通行规则 | 保留徒步深水、森林、飞越及雪地代价 |
| [riding_proficiency.rs](../crates/rfb-core/src/game/riding_proficiency.rs)，坠马候选 | 使用脱离坐骑后的地形资格 | 幽灵可落普通墙，永久墙不合法 |
| [trait_details.rs](../crates/rfb-core/src/game/trait_details.rs) | 已列 Race 穿墙来源 | 总计继续反映骑乘后的有效资格 |
| [item_use.rs](../crates/rfb-core/src/game/item_use.rs)及 `mod.rs` 传送 | 随机/定点目的地读 `is_walkable` | 记录既有简化；本批不得借统一通行放开不可传送墙体 |

## 4. 墙内密度、0 HP、恢复与状态结束

来源：`src/dungeon.c:1512–1654`，`process_world` 的十回合周期（约 3094、3431）；`src/defines.h:330` 的 `TURNS_PER_TICK=10`；`src/effects.c:6377` 的 `take_hit`。

原版每次世界 HP/SP 处理按以下顺序结算：

1. 当前格既没有 `FF_MOVE` 也没有 `FF_CAN_FLY` 才进入墙内分支。这里检查地形的可飞越旗标，不要求玩家此刻真的有飞行能力。
2. `IS_INVULN` 或 `IS_WRAITH` 直接免除这段伤害。
3. 基础伤害 `1 + level / 5`，整数除法。若有 `pass_wall`，`no_passwall_dam` 可免伤；否则为“密度”。本体 `prace == RACE_SPECTRE` 时先令 `dam = min(dam, chp)`。
4. 没有 `pass_wall` 时为“坚硬的岩石”挤压伤害；没有幽灵限额，也不能仅凭 `no_passwall_dam` 免掉此分支。
5. `dam != 0` 才设置 `cave_no_regen` 并调用 `take_hit(DAMAGE_NOESCAPE, ...)`。之后先恢复法力，再通过 `cave_no_regen` 抑制 HP 自然恢复。

必须保留的边界：

- 普通情况下本体幽灵可被扣到 **0 HP**，不是 1 HP；死亡判断是 `< 0`，重写已有对应 [BelowZero 伤害策略](../crates/rfb-core/src/game/damage.rs)和 `player_is_dead`。
- 限额发生在通用 `take_hit` **之前**。该函数还处理舍身等增伤、先见、无双和超越状态的 SP 抵扣；`DAMAGE_NOESCAPE` 不是跳过全部通用机制。不要将源代码的普通情形概括成绝对不死，也不要在最终伤害后再加一层无条件 HP 下限。尚未实现的职业姿态不为本批虚建。
- 在 0 HP 且仍有普通穿墙的本体幽灵，限额令本次 `dam=0`，这段代码不会设置 `cave_no_regen`；其他条件允许时仍可发生自然 HP 恢复。不能简单用“站在墙里”永久关闭恢复。
- 抑制标记取决于传给 `take_hit` 的非零伤害，即使后续被通用效果抵扣也已设置。法力恢复不因这个标记停止；主动治疗也不是自然恢复，不能统一封锁。
- 原版先墙内 HP/SP 处理，再扣状态时限。重写 [turn.rs](../crates/rfb-core/src/game/turn.rs)已在状态 tick 前执行十 tick 墙内结算；剩余 1 tick 的豁免/形态与周期重合已测试。自然 HP 与装备 HP 恢复共用本次非零原始伤害抑制；装备恢复移至自然恢复旁、周期突变之前，无新增持久状态。
- 等待/休息保留已有 SP 恢复入口；休息在伤害/可见敌人中止检查前恢复法力，最后一次受伤回合也恢复 SP。已验证 0 HP 的零伤害可回血，以及超越抵扣成零最终伤害时仍抑制 HP 恢复。

已有形态来源必须分别处理：

| 来源 | 原版规则 | 重写差异 |
| --- | --- | --- |
| 幽灵 Race | 普通穿墙，受上述密度伤害 | 已实现；本体限额在通用受伤前，临时形态无本体限额 |
| `IS_WRAITH` | 穿墙及墙内免伤，另有自身防护 | 保留自身豁免及伤害比例，真实死亡领域施法测试通过 |
| `IS_PASSWALL`（穿壁/野性天赋来源） | `pass_wall` 和 `no_passwall_dam`，见 `src/xtra1.c:4481` | 当前正式能力无此独立来源；待对应入口接入时表达，普通穿墙状态不自动免伤 |
| 恶魔领主 | `src/races_mf.c:208` 设 `kill_wall`，通过破坏墙体前进 | 已移除变形程序的穿墙授予；当前恶魔领主或破墙坐骑按显式可破坏地形前进，真实施法测试通过 |

原版 `set_wraith_form` 结束时（`src/effects.c:3009` 起）重算状态和显示，不强制移出墙体。重写状态结束同样不迁移位置。墙内失去通行后可以离开进入合法格，继续停留则受挤压；禁止凭空新增弹出、传送或存档自动挪位。

保存边界：[validation.rs](../crates/rfb-core/src/game/validation.rs)当前和缓存楼层均允许范围内玩家墙内位置，保留地形元数据、越界和其他实体校验。专项测试覆盖形态结束后原位加载并继续行动、实际地牢离层缓存/返回/再次加载，以及缓存玩家越界拒绝；加载不要求此刻仍可穿墙，也不自动挪位。

破墙使用原版 `FF_HURT_DISI` 对应的显式 `monsterDestroyToTerrainId`，并沿用 `FAF_NO_DROP`：不产出挖矿奖励或熟练度。补齐普通门、秘密门、七级卡门、玻璃墙/门、树和帘的原版破坏目标；透明玻璃允许可破坏，永久地形仍拒绝。破墙与 Wraith 共存时，已能进入且透明的玻璃保留。树与闭帘继续采用包内“不可步行但可飞越”的既有适配，不触发墙内伤害或 150 误加价。其他项目自定义地形未有明确破坏元数据时不推测补值，不据此声称所有自定义墙都已等同原版。

## 5. 出生、饮食及现有共用消费者

第三步验证：[spectre_supplies.rs](../crates/rfb-core/src/game/tests/spectre_supplies.rs) 六个专项、饥饿/照明/穿墙 55 个既有核心测试及一个导入测试通过；`inventory / potions / town / progression` 四类 15 个契约通过，无需刷新。内容锁、Rust 格式、核心与导入 crate 的 Clippy 检查通过。未改前端或公共协议，正式创建和桌面验收仍留第五步。

出生法杖由 `src/py_birth.c:97` 调 `device_init_fixed`；`src/devices.c:3010` 起明确设置满能量。名称沿用原版 `src/bldg.c:2527,2550` 的“空手法杖”，现有中文内容已一致；激活名称为 `devices.c` 的“无”，不能将物品另译成“无用法杖”。第三步已复用 `fullyCharged` 起始物品、[initialization.rs](../crates/rfb-core/src/game/initialization.rs)的夜间标签入口和照明流程，并将幽灵加入普通口粮排除。六职业真实初始化均验证一根满能量法杖、3–7 根火把及原有职业装备；测试内容副本临时放行种族创建，正式包不开放。

原版 `src/cmd6.c` 的装置吸能消耗能量、增加 5000 营养，保留物品；重写 [inventory.rs](../crates/rfb-core/src/game/inventory.rs)已复用于幽灵的当前 `device-eater` 资格。实际命令覆盖出生法杖、背包/脚下、部分能量、耗尽后耗时但不增加营养，以及魔法商店购买首个投影装置后吸能；保存加载后继续操作的事件和状态一致。

普通食物按当前种族营养除以 20；药水 `src/cmd6.c:620` 在没有临时变形且本体幽灵时才除以 20，有变形则走通用分支。已复用 [item_use.rs](../crates/rfb-core/src/game/item_use.rs)的营养除数和形态分支，实际使用测试覆盖本体幽灵、人类变幽灵、幽灵变人类及本体幽灵仍有临时幽灵形态；验证普通口粮、行粮治疗/解毒、蘑菇治疗/恢复，以及药水正负营养、取整和主效果。临时形态不重新发出生装备或切换出生时钟。

原版旅馆买食物（`src/bldg.c:2517` 起）也有非凡人饮食文案，最终直接设饱食为 `PY_FOOD_MAX - 1`；文案中的法杖不是可带走的物品奖励。重写 [town.rs](../crates/rfb-core/src/game/town.rs)已有住宿/旅行，但没有这项独立餐饮服务，继续记录缺失，不在出生或商店购买中模拟它。

其他现有消费者：

- [virtues.rs](../crates/rfb-core/src/game/virtues.rs)：幽灵已补原版种族美德“非生”；测试检查出生槽位次序，临时形态不重抽出生美德。
- [monster_ecology.rs](../crates/rfb-core/src/game/monster_ecology.rs)：怪物恐怖使用当前不死形态的 `25 + level` 额外豁免，与原版 `src/monster2.c:2425` 相符；临时幽灵的成功/失败边界已测试。不能用旅馆噩梦的 `10 + level` 去“修正”这一常数。
- [monster_combat.rs](../crates/rfb-core/src/game/monster_combat.rs)：实际近战测试确认幽灵仍受吸血攻击伤害，但攻击者不回血；UNLIFE 不扣幽灵生命力或增强攻击者。普通人类对照保留回血/扣除；保持生命不能当成生命力槽或普遍伤害免疫。生命力耗尽后的永久变种族尚缺，见下一节。
- [item_use.rs](../crates/rfb-core/src/game/item_use.rs)及 [monster_abilities.rs](../crates/rfb-core/src/game/monster_abilities.rs)：当前幽灵不受盐水饥饿/麻痹效果或无空气状态影响；与普通人类对照已测试，食物/药水主治疗效果延续第三步验证。
- 当前种族抗性、识破隐形、浮空、缓慢消化和同族类别均有现有消费者；第二至四步按各自实际效果验证，不仅检查 JSON 出现字段。

## 6. 全部直接 `RACE_SPECTRE` 引用分类

本次用 `git grep -n RACE_SPECTRE master -- src` 枚举并逐处核对；种族函数另读 `races_k.c`。直接引用之外的旗标和当前种族通行规则已在前文单列。

| 原版位置 | 含义 | 本批归属 |
| --- | --- | --- |
| `defines.h:620`；`races.c:104,449`；`py_birth.c:1024` | 索引、英文别名、构造器、出生不死分组 | 保留稳定身份，第五步已开放 |
| `cmd6.c:620` | 无临时形态时药水营养特例 | 第三步 |
| `dungeon.c:1538` | 本体幽灵密度伤害限额 | 第二步 |
| `spells3.c:4503` | 同族 `G` | 第四步复用并验收通过 |
| `virtue.c:430` | 种族美德非生 | 第四步已补 |
| `bldg.c:2346` | 旅馆噩梦的不死 `10 + level` 豁免，含本体/变形特例 | 当前旅馆无相应噩梦服务；记录依赖，不虚建服务 |
| `cmd1.c:309` | `death_scythe_miss` 反噬倍率中的本体无变形分支 | 是特定武器反噬，不能添加为所有近战的幽灵伤害倍率；待该武器规则接入 |
| `effects.c:5763` | `lp_player` 生命力耗尽后，非怪物玩家随机转为五种不死种族之一（含幽灵）；非生物重置生命力，否则死亡 | **已有生命力扣除，但终点转化缺失。** 第四步保留此缺口，范围与验收条件见下文 |
| `doppelganger.c:176` | 变形怪幽灵菜单项 `{45,80,RACE_SPECTRE}` | 完整菜单和维持机制未接入；本批只验当前通用临时形态消费者 |
| `files.c:3024` | 狂战士计分考虑本体、历史出生和旧种族记录中的幽灵 | 狂战士未开放，随该职业计分处理；不得误用于 HP 判断 |
| `troika.c:949` | 已是幽灵时的奖励文案分支 | 未开放职业依赖，无独立幽灵奖励规则需本批新造 |
| `shop.c:93,128,129,198,266,274` | 六位店主的种族元数据 | 保持数据，不推导额外玩家种族能力 |

生命力终点转化源头位于 `src/effects.c` 的 `lp_player`：普通玩家生命力耗尽随机选择吸血鬼、骷髅、僵尸、幽灵或英灵并 `change_race`；转换后当前种族为非生物则重置到 1000，否则因转换失败承受致命伤害。怪物职业不能如此改种族，生命力留在 0。当前 `progression.rs` 的 `apply_life_force_drain` 只减到 0，`drain_player_life_force` 重算 HP，没有对应永久转换。

第四步决定保留该已知缺口。当前临时变形通过状态覆盖种族，不能代替永久 `change_race`；随机池还含尚未完整接入的吸血鬼。后续实施必须保留五种目标及其概率，在本体切换中核对技能/成长、身体装备、美德、已有临时形态与相关状态，随后执行非生物生命力重置或转化失败死亡。验收须包括生命力恰到 0/跨过 0、五种目标、变形中耗尽、转化失败、保存加载及 RNG 重放；未开放怪物职业的分支随职业接入验证。不得排除吸血鬼以假装完成，也不添加 1 点生命力保底或旧存档兼容。

后续具体顺序、经验模型差异、五目标依赖和验收条件见[生命力耗尽永久转化实施计划](life-force-race-change-plan.md)。该计划尚未实施，不能据此将本节缺口计为已修复。

## 7. 后续验收清单与范围

第四步验证：[spectre_rules.rs](../crates/rfb-core/src/game/tests/spectre_rules.rs) 七个专项及 37 个相关既有核心测试、一个导入测试通过；`progression / combat / status-effects / scrolls` 四类九个契约通过，无需刷新。测试覆盖真实升级、自然随机施法成功/失败及保存继续施法；仅免疫/遮挡隔离检查固定施法成功，形态和高等级同族测试使用显式前置条件。正式入口与桌面验收仍留第五步。

第二步已完成下表通行、移动消费者、结算、来源及形态、恢复和连续性的专项覆盖；第三步已完成出生/营养及保存继续行动；第四步已完成恐吓能力、美德、同族与现有不死消费者的专项覆盖。永久转化按第六节保留缺口：

| 组别 | 必须覆盖 |
| --- | --- |
| 通行 | 普通墙成功；永久墙/边界拒绝；树木无密度且无 150 误加价；可飞越山地；占位、视线不变 |
| 移动消费者 | 实际移动、寻路、局部荒野/世界地形、坐骑双方资格、下马/坠马落点一致 |
| 结算 | 成功穿墙能量 150；世界十 tick、移动/等待/休息；等级 4/5、HP 高于/等于/低于基础伤害；0 HP 与 `<0` |
| 来源及形态 | 本体/临时幽灵、幽灵本体其他形态、穿墙全失、无敌、Wraith、明确免伤来源、恶魔领主破墙差异 |
| 恢复 | 非零墙内伤害抑制当次 HP；0 伤害边界；SP 及主动治疗；装备恢复；时限结束与周期同 tick |
| 连续性 | 墙内存档、形态墙内结束、离层缓存、载入后继续行动；相同前置与 RNG 下结果一致 |
| 出生/营养 | 六职业夜间装备；满能量法杖与照明；无多余口粮；背包/脚下/部分/耗尽吸能；食物和药水的本体/形态分支 |
| 能力/关联 | 3/4 级解锁、智力失败与 SP/HP 支付、真实恐惧及抗性/遮挡；当前形态能力与同族 `G`；非生美德与已有不死消费者 |

第五步已开放创建、补中文分点说明并完成六职业连续核心流程及三职业 standalone Tauri 桌面抽查。桌面实际操作和测试中注入等级/前置条件分开记录，见下节。

第二步已更新内容包版本与 lock；没有改变状态哈希输入或公共协议结构，不升级 State Hash Schema。后续按实际变更运行相关测试与契约；只有公共初始化、RNG、共享投影或状态哈希输入等确有变化时扩大范围，遵守 Schema v62 后的项目规则。不兼容旧开发存档，不创建未开放职业/服务，也不把已知关联缺失称为完成。

## 8. 第五步最终验收

正式入口共 46 个，内容包版本及哈希以 [pack.json](../packs/rfb-demo-original/pack.json) 和 [content.lock.json](../packs/rfb-demo-original/content.lock.json) 为准。前端创建页保留原版中文说明，并与角色详情共用八条特性文案；英文占位已替换。

- 核心：`game::tests::spectre` 14 个、`game::tests::wall_passage` 12 个通过。新增连续流程通过正式入口创建六职业，吸收出生法杖、通过经验管线上升至 4 级、自然随机恐吓生效、150 能量入墙、等待密度伤害、离墙、保存恢复及继续行动结果一致。测试显式授予经验、补满 HP、布置地形和羊，按真实施法事件检查恐惧，避免把结算前已消退的短状态误判为未施加。低生命和形态结束边界沿用并复验既有穿墙测试。
- 检查：1 个导入测试、17 个相关前端测试、内容 source/lock、TypeScript、Rust 格式及 `rfb-core / rfb-legacy-import` Clippy 通过；此次最终验收运行全部 26 个 active 契约，全部通过，无基线刷新。
- 桌面：`node e2e/tauri.e2e.mjs --spectre` 在独立 WebDriver Tauri 构建中通过战士、死亡高阶法师、弓箭手；三者均验证中文创建/详情八条分点、出生法杖/火把/无口粮、吸能 21→20 与营养 99%→149%、装备卸下再穿上、真实入墙/等待受伤/离墙、保存精确恢复和继续行动。运行保持 1 级，没有注入桌面游戏状态，也没有将核心的升级与恐吓结果写作桌面完成。

| 桌面职业 | 入墙前 HP | 入墙 HP | 墙内等待 HP | 离墙坐标 |
| --- | --- | --- | --- | --- |
| 战士 | 43 | 42 | 41 | `(44,14)` |
| 死亡高阶法师 | 19 | 18 | 17 | `(44,14)` |
| 弓箭手 | 35 | 34 | 33 | `(44,14)` |

运行记录在忽略目录 `test-results/spectre-acceptance.json`；创建页、详情、墙内截图分别为 `spectre-<build>-creation.png`、`spectre-<build>.png`、`spectre-<build>-wall.png`，已目视检查布局。普通试玩产物为 `target/debug/rfb-tauri.exe`，由 `npm run build:standalone:debug` 生成并内嵌前端；专用自动验收产物为 `target/e2e/debug/rfb-tauri.exe`，由 `npm run e2e:build` 生成。两者均已完成构建，不依赖 Vite 开发服务器；实际脚本验收使用后者。

上述完成范围不包含第六节的生命力耗尽永久转化，以及尚未开放的变形怪菜单、狂战士、Troika、噩梦服务或特定武器反噬。
