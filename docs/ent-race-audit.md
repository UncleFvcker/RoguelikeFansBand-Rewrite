# 树人接入审计

核对日期：2026-09-09。当前实现基准：`ad8c1a3a5`。原版来源：`D:/codex/Frogcomposband/master` 的 `master`，本次解析为 `a0d92b6378d148c5262cc236b8fa6ed2ca06a54c`；下文原版文件和行号均指这个 Git 对象，不指工作树文件。

首次审计提交 `568df8270` 完成树人计划第一步：核定来源、当前调用路径和后续范围，仅作源码与内容审计。下文差异表保留这一基线，实施前按实际 HEAD 核对相关变化。

第二步已落实（内容包 `1.391.0`）：26/41/46 级成长、毒抗验证、火抗百分比与背包豁免、无近战武器时的挖掘加成、步行及骑乘穿林。原版 `obj_kind.c::object_is_melee_weapon` 包含挖掘工具，装备铲子也会关闭种族挖掘加成。成长按实际等级派生，跨级升级事件使用各级体质计算生命值。森林通行复用现有木精灵路径，修复两种族的骑乘遗漏并保留水生坐骑限制。出生与主动能力尚未接入，正式入口仍关闭。

第三步已落实（内容包 `1.392.0`）：当前树人形态的普通食物营养除以 20，精灵干粮仍先治疗与减毒，再增加 375 营养。现有 66 种药水逐项按原版 `k_info.txt` 与适配清单保留有符号 `pval`，导入器和内容 schema 同步支持 `potionNutrition`。真实饮用路径在主效果之后结算营养：未变形的原生树人使用特殊公式及 14999 上限，临时树人/树人临时变成人类使用原始药水营养。水增加 4200，零 `pval` 药水增加 2000，无敌药水的 `pval=-2500` 使树人净减少 500；盐水先执行饥饿、麻痹和解毒，再补至 2099。普通已开放种族也恢复药水基础营养（魔像、僵尸沿用 1/20，骷髅不获营养），不将药水套入树人的食物除数。

出生改为 15～23 瓶水，沿用 3～7 支火把与 1500～3500 燃料；水超过现有每堆 20 瓶上限时分堆，保留六职业装备并支持保存恢复。六职业各 16 个种子覆盖全部出生水量，测试目录中临时开放树人用于真实初始化验证；正式入口继续等待第四、五步。

第三步再次核对关联生成：`worlds/middle-earth.json` 的 `demo.guaranteed.warrens-food` 是通用 `guaranteed_items` 配置，经 `game/world/generation.rs` 原样生成指定物品；通用掉落表也含口粮。它们未定义原版 `ALLOC_TYP_FOOD` 类别，不能据物品 ID 或配置名称批量改成水。`game/town.rs::stay_at_inn` 恢复生命、资源、状态与设备，不修改营养，服务枚举无 `BACT_FOOD`。这两个原版消费者仍缺失；本步已验证杂货店真实库存购买水后可饮用。

药水基础营养带来的契约变化仅刷新 `potions` 类别的一份新生药水场景：主效果后增加原版 100 营养及相应饱食状态事件。内容 hash 仍不参与状态 hash；未改状态结构或全局 RNG。

第三步验证：核心饮食 30、物品 50、照明 13、城镇 39 项测试通过；导入器两项树人/药水测试通过；前端事件格式 30 项通过。所选 `inventory / potions / progression / town / equipment / status-effects / system` 共 20 份契约通过；内容锁验证、schema 同步、格式检查及三个相关 Rust crate 的全目标 clippy 通过。共用营养消息改为“补充饱食度”，兼用于食物与饮用。未运行桌面 E2E，未开放正式树人入口。

第四步已落实（内容包 `1.393.0`）：接入“召唤树人”的原版名称、说明、10 级/感知/20 消耗/70 基础失败率及导入映射。复核发现首次审计遗漏 `SPELL_COST_EXTRA`：45 级起额外消耗 30，实际基础消耗为 50；使用现有种族能力费用增长配置，费用、失败率、SP 不足时的 HP 支付仍走共同管线。

植树复用 `CreateAdjacentTerrain` 的落点过滤、地形写入及事件。低等级在成功施放后按原版 `randint0(9)` 执行最多五次尝试；5 不计次、0 浪费一次、9 不抽取；全失败仍支付资源并显示原文“没有树人响应召唤。”。45 级起尝试全部八个相邻格，无落点 RNG；空结果仍成功。低等级的 `in_bounds` 排除地图最外圈；高等级投射的 `in_bounds2` 允许地图内最外圈的合法地面。两分支都排除玩家格、地面物品、金币、怪物、连接点及不符合原版地面标志的地形。植树没有生成怪物或宠物。

植树源地形清单覆盖当前 12 个对应定义：普通地板、泥土、荒野泥土、草地、浅水、花地、灌木、沼泽、玻璃地板、泥泞、雪地、滑冰面。其中 `dirt` 与 `surface-waste` 都承接原版 `DIRT`，后者不是 `SHALLOW_WASTE`；原版同样带 `FF_FLOOR` 的“毒酸液”尚无正式地形定义，本步不新建这一关联消费者。现有自然领域的森林能力保留自己的地形清单，本步未据此宣称全领域地形行为一致。

种族出生美德加入“自然”；临时变形不重抽美德。现有同族召唤卷轴经当前种族 `kin-glyph-35`（`#`）正常筛选并生成友军，已验证原生与临时树人及退出形态。德墨忒尔/树人的关系例外集中到 `monsters_are_enemies`，普通怪物、宠物、法术落点与射线友军风险、让位路径均复用它。非树人形态下两者不因敌我身份不同而交战；当前树人形态仅跳过该例外，仍依据敌我身份决定，双方同时受玩家控制时保持友军。原版更早的相反善恶阵营分支对当前这两个定义不适用（树人为善，德墨忒尔中立），测试固定了这一前提；未借本步扩充全局善恶阵营战斗规则。

第四步未接入关联仍包括：原版“液态洛格鲁斯”药水、变形怪形态菜单和维持规则、律师与法律领域适性、`wild_talent` 借用植树的职业入口，以及前述缺失的毒酸液地形。现有通用临时变形可获得和失去本次种族能力。正式树人入口继续留待第五步。

第四步验证：新增七项核心测试，覆盖能力等级/费用/感知/失败支付/临时形态、原版方向抽样及 RNG、12 个现有地面定义、占用与两种边界规则、视线/通行/保存后继续行动、同族卷轴、双向敌对与宠物/法术友军风险/让位。相关怪物 AI、怪物移动、地形能力、召唤能力、美德五个完整模块共 54 项通过；出生、饮食及被动也经首轮相关回归验证。新增导入器测试、31 项前端事件格式测试、TypeScript 类型检查、格式检查、相关 Rust 全目标 Clippy 及内容锁验证通过。`movement / progression / combat / inventory / equipment / status-effects` 共 12 份现行契约通过，无需刷新；没有修改协议或状态哈希结构，未运行完整桌面 E2E。

## 第五步已完成：正式入口与专项验收

树人已加入正式 `rfb-compatibility` 标签、导入器映射、前端白名单及创建选项；正式入口由 44 增至 45，仍为六职业。内容包为 `1.394.0`，内容锁 `afc8e611bdd81361c16817abf9e66e10759d88e2ea58ac08242a10f91b695eb5`。原版 `master` 仍为本文开头所列对象，中文名称及完整说明保留原文；英文临时变形占位说明已替换。

创建页与角色详情共用八条本地化特性文字：基础、成长、挖掘、火抗、饮食、穿林、主动能力、出生。角色详情将种族说明放在抗性与能力页的通栏折叠项中，避免八条说明挤入抗性网格的窄列；创建页保留 `aria-describedby`。

### 核心自动验证

- 移除第三步临时修改内容标签的测试目录，出生测试直接调用正式 `Game::new_with_build_race_and_name`。六职业各 16 个种子，共 96 次真实出生，覆盖 15～23 瓶水、超过 20 瓶时分堆、无口粮、照明、完整职业装备及保存恢复。
- 新增一个连续流程测试，依次运行战士、死亡高阶法师、弓箭手、死亡圣骑士、骑兵、狙击手：使用出生水 → 通过经验授予路径升至 46 级 → 卸下/装备/再卸下出生武器 → 施放“召唤树人” → 进入新树格 → 保存/恢复 → 双方继续移动并比较完整投影及状态哈希。挖掘贡献在 0 与 460 间切换，喝水消耗一瓶并从 1000 补至 5200。
- 连续流程设置安全场地、授予经验并固定施法成功，不冒充正常游玩升级或失败率验收；相邻测试覆盖实际失败/资源支付/落点 RNG、10 与 45 级分界，成长模块覆盖 26/41/46 级与降级/变形。
- `game::tests::hunger::` 30 项、`game::tests::abilities::terrain::` 13 项、`game::tests::progression::` 53 项，共 96 项通过；导入器 `content::tests::ent_` 两项通过。
- 前端 `session-shell / character-traits-panel / event-format` 共 43 项通过，TypeScript 与 E2E 脚本语法检查通过。相关 Rust 全目标 Clippy、格式检查、内容锁验证通过。
- `movement / progression / combat / inventory / equipment / status-effects / potions / town` 共 19 份契约通过，无刷新。正式标签仅影响创建资格，不改变已有变形候选池、共用初始化 RNG、协议或状态哈希结构。

### 桌面验收

专项命令：在 `web` 执行 `npm run e2e:build`，然后 `node e2e/tauri.e2e.mjs --ent`。构建经 `tauri build --debug --no-bundle --features webdriver` 内嵌 Vite 产物，是独立 Tauri 应用；专项仅选择树人三职业，不执行整套桌面 E2E。结果与截图位于 `test-results/ent-acceptance.json`、`ent-*-creation.png`、`ent-*.png`。

2026-09-09 三职业全部通过，均使用种子 83、新存档：创建名称“树人”、中文原文、八条分点说明与无横向溢出、角色详情投影、出生水/火把/无口粮、饮水、卸装/重装、导出 `.rfbsave`、加载恢复精确状态哈希、继续行动及无前端错误。饮水后的水量分别为战士 19→18、死亡高阶法师 16→15、弓箭手 15→14，饱食度显示均从 99%→141%。三职业详情截图及创建页截图已检查。首次脚本因饮水后水瓶仍勾选而进入多选，修正测试选择步骤后完整重跑通过；未修改背包的正常多选行为。

桌面抽查停留在出生等级，不声称在桌面正常游玩升至 10/45/46 级；升级、植树、穿林及其保存后的连续行为由上述核心专项覆盖。普通试玩产物另经 `npm run build:standalone:debug` 成功生成：`target/debug/rfb-tauri.exe`（无 WebDriver 特性、内嵌前端，无需 Vite 开发服务器）。桌面抽查使用相同源码和内容的 `target/e2e/debug/rfb-tauri.exe`，与普通试玩产物区分记录。未运行完整桌面 E2E、全量契约或整工作区测试。

### 仍未接入的关联

正式开放不扩展此前明确缺失的 `ALLOC_TYP_FOOD` 地牢专用食物分配、`BACT_FOOD` 餐饮服务、液态洛格鲁斯、变形怪形态菜单/维持、律师/法律领域、`wild_talent` 职业入口及毒酸液地形。现有商店购水已由核心真实交易路径验证。

## 首次审计：已具备的内容与基础

| 项目 | 核对结果 | 当前依据 |
| --- | --- | --- |
| 稳定身份 | `rfb-legacy.race.ent`、原版索引 30；沿用现有定义 | [ent.json](../packs/rfb-demo-original/races/ent.json) |
| 中文 | 名称“树人”和完整说明逐字符合原版 `src/races_a.c:1774`；无需重新翻译 | [中文内容](../locales/zh-CN/content.ftl)的 `race-legacy-ent-*` |
| 基础属性 | 力量 +2、智力 0、感知 +2、敏捷 -3、体质 +2、魅力 0 | `ent.json`；原版 `src/races_a.c:1803` 起 |
| 生存与经济 | 生命 105%、基础生命 25、经验 135%、红外视觉 0、商店调整 95% | `ent.json`；原版 `src/races_a.c:1785` 起 |
| 技能 | 原版 `dis/dev/sav/stl/srh/fos/thn/thb` 分别为 `-5/+1/+5/-1/0/+9/+15/-3`；零项由默认值表示 | [race-ent.json](../packs/rfb-demo-original/skillSets/race-ent.json)；原版 `me.skills` |
| 毒抗 | 已定义一层毒抗，通用种族抗性合并路径可消费 | [player_stats.rs](../crates/rfb-core/src/game/player_stats.rs)的 `effective_player_resistances` |
| 同族召唤 | 原版 `src/spells3.c:4509` 使用 `#`；已有 `kin-glyph-35`，通用召唤解析读取当前种族的 `kin_category` | [导入器](../crates/rfb-legacy-import/src/content.rs)的 `legacy_race_kin_glyph`；[item_use.rs](../crates/rfb-core/src/game/item_use.rs) |
| 身体与变形基础 | 已有 15 个项目标准装备槽及 `polymorph-candidate`；通用变形可引用此定义，但不代表树人特殊规则完成 | `ent.json`；[progression.rs](../crates/rfb-core/src/game/progression.rs)的 `character_definitions`；[monster_combat.rs](../crates/rfb-core/src/game/monster_combat.rs) |
| 补给物与地形 | 水、商店库存、树木地形及相邻地形生成器已存在 | [water-potion.json](../packs/rfb-demo-original/items/water-potion.json)、[杂货店](../packs/rfb-demo-original/shops/outpost-general-store.json)、[surface-tree.json](../packs/rfb-demo-original/terrain/surface-tree.json)、[森林生成程序](../packs/rfb-demo-original/abilityPrograms/nature-forest-creation.json) |

首次审计时尚未开放：定义没有 `rfb-compatibility`，核心创建检查会拒绝正式树人开局；前端白名单也没有树人，英文说明是临时变形占位文字。上述缺口现已在第五步关闭。

## 第二步：成长与被动缺口

| 原版规则 | 当前差异与最小接入位置 | 必须验证的边界 |
| --- | --- | --- |
| 26、41、46 级各增加力量和体质 1、减少敏捷 1；由 `ent_get_race` 每次按等级重算 | 当前只有一级静态属性。接入 `progression.rs::player_attributes_with_sources` 的种族属性来源，复用现有属性计算，不另存成长累计值 | 25/26、40/41、45/46；升级、降级、变形进入/退出；面板与体质相关生命值一致 |
| `_ent_calc_bonuses`：未装备任何近战武器时，挖掘技能增加 `等级 × 10` | `player_stats.rs` 当前无树人分支；复用装备定义与挖掘属性管线。判定是“没有近战武器”，不要求所有手部和射击槽为空 | 空手、盾牌、发射器、单手/双持武器、卸装、临时形态 |
| `resist.c:332`：合并后非负且未达到免疫的火抗百分比乘 7 再整数除 10 | `adjust_player_resistance_percent` 已承接冬贝利同类特殊计算，可直接补本种族分支；当前无树人火抗折减 | 零抗、正抗、多层、负抗、免疫；真实伤害与已知投影同源 |
| `resist.c:435`：背包火焰豁免的随机上界从 66 改为 54，比较值为树人调整后的火抗百分比 | `inventory.rs::inventory_resistance_save` 当前使用通用上界和未调整的枚举抗性；两个部分均须补齐 | 带/不带火抗、免疫、背包保护、物品固有防毁；不影响其他元素和种族 |
| `cmd1.c:5176` 起：树人免除穿越树木的双倍行动消耗 | 可复用 `forest-adapted`。其函数虽在 `wilderness.rs`，实际被普通移动、荒野和 `travel.rs` 共同调用；当前树人缺此标签 | 手动移动与寻路、地牢与荒野、临时形态、骑乘时的落点与消耗 |

火焰规则不是直接追加 `vulnerable`：无抗仍为 0%，例如普通 50% 火抗变为 35%；免疫和负抗绕过该折减。背包豁免也不能仅改常数而继续使用未折减百分比。

现有普通玩家对树木的通行方式与原版并不完全相同，且当前 `forest-adapted` 通行函数限定未骑乘。树人批应验证自身的直接调用路径；若需要改动共同移动行为，单独说明受影响范围，不据此宣称全种族地形规则已一致。

## 第三步：饮食、出生与获取

| 原版规则 | 当前差异与本批处理 |
| --- | --- |
| `cmd6.c:70`、`:440`：当前树人形态不属于正常凡人饮食，普通食物营养为 `pval / 20`，食物本身的治疗等效果仍执行 | 已有 `food_nutrition_divisor` 和特殊食物消费者；树人尚未配置。验证普通食物、特殊饱腹食物与解除状态的顺序，不把所有营养效果都除以 20 |
| `cmd6.c:597–666`：饮用后的营养另有分支；未变形且出生种族是树人时，设为 `min(当前营养 + pval + max(0, pval × 10) + 2000, 14999)` | 当前水程序只有 `no-numeric-effect`，没有树人饮用营养。需要在真实饮用路径接入，并保留原版 `pval`；药水主效果先发生。不要改成仅水瓶专属，也不要直接复用普通食物除数 |
| `lib/edit/k_info.txt:2758`：水的 `pval` 为 200 | 原版树人饮水单次补充 4200，随后应用 14999 上限；水物品和可购买库存已存在，复用稳定 ID |
| `_ent_birth`：15～23 瓶水，随后 `py_birth_light`；不调用默认口粮函数 | 当前 `hunger.rs::starting_ration_quantity` 不排除树人；若直接开放会仍给普通口粮。替换其种族补给，避免“口粮加水”重复发放；照明复用现有路径 |
| `dungeon.c:6444–6470`：职业发装备，种族发食物和照明；`py_birth_light` 给 3～7 支火把，燃料为随机 3～7 乘 500，忍者例外 | 当前初始化已分配口粮/照明并合并职业、种族、个性和 Build 物资。六个已开放职业逐一检查，保留职业武器、护甲、弹药和法术书；不扩充未存在的忍者职业 |
| `generate.c:399`：专门的 `ALLOC_TYP_FOOD` 放置对树人选择水，其他种族选择口粮 | 当前地牢通过通用战利品表生成物品，未找到对应的独立食物分配类别。记为该生成分支缺失；不能把所有口粮掉落、商店库存或任务奖励一概替换为水 |
| `bldg.c:2520` 起：`BACT_FOOD` 餐饮服务对树人有供水说明 | 当前有商店购水与住宿入口，但没有对应 `BACT_FOOD` 餐饮服务种类。保留关联缺口；不把住宿或普通购买冒充这一服务 |

**出生种族和当前形态不能混用。** `races.c::prace_is_` 使用当前变形；普通食物、火抗和森林判断沿用该含义。但原版饮用营养先检查 `mimic_form`，仅在 `MIMIC_NONE` 下再检查出生种族。临时变成树人并不会命中上述特殊饮用分支，而会走变形的默认药水营养。后续应按此分别测试，不能把全部树人效果统一套到 `character_definitions()`。

本阶段验证重点：水与非水药水、特殊药水主效果顺序、营养上限（包括原本已高于 14999）、出生水量与无额外口粮、六职业装备合并，以及正式购买后可实际饮用。公共初始化或 RNG 消耗若发生变化，按实际影响扩展契约验证。

## 第四步：主动能力及关联

### “召唤树人”实际创建地形

来源：`src/races_a.c:1745`、`src/spells_s.c:1251–1323`、`src/spells2.c:4147`、`src/spells1.c:1244`、`src/defines.h:3799/3851`、`src/tables.c:24`。

- 解锁等级 10，属性 `A_WIS`，消耗 20，基础失败率 70；`SPELL_COST_EXTRA` 在 45 级起增加 30（第四步复核补充），后续真实费用和失败率走现有种族能力管线。
- 中文名固定为“召唤树人”。原版等级说明使用“一只/许多树人”的文字，但执行结果是 `feat_tree` 地形，不能创建 `demo.actor.ent` 或宠物。
- 45 级前最多消耗五次落点尝试；每次调用 `randint0(9)`，抽到 5 跳过且不计尝试。0 对应玩家格，会浪费一次尝试；方向 9 不会被抽到。不能擅自改成均匀八方向或从所有合法空格中保证选中一个。
- 落点必须在边界内且满足 `cave_naked_bold`：有 `FF_FLOOR`、无 `CAVE_OBJECT`、无地面物品、无怪物、不是玩家格。全部尝试失败仍返回施放成功并显示“没有树人响应召唤。”，不是退还行动或资源的取消。
- 45 级起调用 `tree_creation()`：半径 1 的 `GF_MAKE_TREE` 投射，继续检查空裸地面和玩家格；可以无地形变化而仍完成施放。
- 当前导入器 `parse_race_powers` 没有 `summon_tree_spell` 映射，种族没有能力条目。现有 `CreateAdjacentTerrain`、占用过滤和 `apply_adjacent_terrain_creation` 可复用；低等级的尝试/RNG 语义需补，高等级还要核对可替换地面范围。现有自然领域程序只列普通地板，不能据此断言覆盖原版所有 `FF_FLOOR` 地形。

验证：9/10 与 44/45 级、成功/失败/空结果资源消耗、原版方向抽样、地图边界、物品/金币/怪物占用、楼梯等地形、生成后的通行与视野、存档恢复。

### 关联逐项处置

| 原版引用 | 当前状态 | 本批范围 |
| --- | --- | --- |
| `virtue.c:387`，种族美德 `VIRTUE_NATURE`，中文“自然” | 共享美德系统和其他种族映射已存在，树人缺映射 | 补齐；按出生身份初始化，不因暂时变形重抽美德 |
| `spells3.c:4509`，同族召唤 `#` | 导入和通用运行消费者已具备 | 保留并验证，不新增第二套映射 |
| `monster1.c:864`，`MON_DEMETER` 与 `MON_ENT` 的敌对关系特例 | 两种怪物和召唤已存在；`movement.rs::monster_hostile_targets` 等现有目标选择只按玩家阵营区分，无此特例 | 属于已有消费者缺口，应在第四步补齐并覆盖双向攻击/宠物路径。原版先处理敌对阵营，再处理此特例；非树人玩家时该怪物对在这一分支不互相敌对，树人玩家时跳过特例。不是树人玩家自动获得友善或宠物 |
| `devices.c:494–561`，`SV_POTION_LIQUID_LOGRUS` 的临时树人分支，持续 `20 + randint1(20)`，另有变形免疫替代行为 | 中文名“液态洛格鲁斯”可从原版中文表解析；当前只有历史导入计划记录，没有正式物品/效果消费者 | 记录依赖缺失。本批确保既有临时形态能消费树人被动，不为此扩充整瓶随机药水 |
| `doppelganger.c:148`，变形怪的树人选项 `{11, 40, RACE_ENT}` | 有变形怪基础 Race，未有完整形态选择/维持规则 | 记录依赖缺失，不等同于通用随机变形候选 |
| `lawyer.c:3/33`，树人对 `REALM_LAW` 的适性调整 | 当前无律师职业与该领域的正式能力链 | 留给关联职业/领域；不能仅因函数也处理死亡法术就给当前死亡领域额外树人加成 |
| `wild_talent.c:128`，借用同一植树函数的能力 | 当前未有对应职业能力链 | 只复用树人本次实际需要的执行路径，不新增该职业 |
| `devices.c:7381`，改变性别效果中的“现在是个树人” | 紧接“开玩笑”的文字；没有种族赋值或变形调用 | 排除伪缺口，不实现永久变树人 |
| `shop.c:119/121` 的两名树人店主，`py_birth.c:1026` 分类菜单，`defines.h`/`races.c`/`externs.h` 的身份注册 | 分别为店主数据、菜单分类和原版注册 | 不视作玩家被动。入口沿用当前菜单体系；不借本批扩充店主轮换 |

## 首次计划：第五步入口与验收边界

前述核心规则完成后，再补正式标签、核心创建和前端白名单，采用原版中文说明并分点展示种族特性；英文占位说明同时修正。六职业出生与饮水、成长、换装、植树、穿林、保存恢复和继续行动作核心验收，standalone Tauri 桌面分别抽查战士、死亡高阶法师和弓箭手。

未有的餐饮服务、独立地牢食物分配、液态洛格鲁斯、变形怪和相关职业关联，须继续作为明确缺口列出；具备基本树人玩法不等于全原版关联覆盖。原计划中“地牢食物生成与旅店补给”据本次审计细化为：当前可购买的水供应必须打通，尚无对应消费者的两个原版分支不在树人批凭空搭建。

第一步检查仅包括原版 Git 引用、JSON/中文基线和文档引用；该审计提交未修改运行代码或内容包，也未运行行为测试。第二步的验证见对应实现提交说明；协议、状态哈希结构和存档格式未变。
